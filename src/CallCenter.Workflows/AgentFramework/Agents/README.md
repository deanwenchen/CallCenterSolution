# Agent Framework Agents

Put Agent Framework-native agents here.

Examples:

- `IntentAgentFrameworkAgent`
- `PlannerAgentFrameworkAgent`
- `RefundAgentFrameworkAgent`
- `HumanReviewAgentFrameworkAgent`

Agents in this folder may depend on Agent Framework types. They should not contain direct database, HTTP, or external-system code.
