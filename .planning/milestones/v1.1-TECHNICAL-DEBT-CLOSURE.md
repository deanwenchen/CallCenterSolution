# Milestone Archive: v1.1 Technical Debt Closure

**Status:** SHIPPED 2026-06-01
**Phases:** 5-8
**Total Plans:** 7

## Overview

v1.1 Technical Debt Closure completed all remaining v1.0 requirements: intent switching, agent pipeline with compaction, audit logging with saga compensation, and business extensibility documentation.

## Phases

### Phase 5: Intent Switching + Timeout

**Goal:** 实现意图切换（退款中改换货）、异常输入重新识别、30 分钟超时提示
**Plans:** 1 plan

Plans:
- [x] 05-01: Fix timeout, intent switching, confirm re-recognition

**Details:**
- CheckTimeoutAsync 30-min warning no longer clears activeWorkflow (CD-04)
- IntentSwitchResult handler prints clean Chinese messages (IR-04)
- HandleRequest→HandleRequestAsync with intent re-recognition callback (IR-05)

### Phase 6: Agent Pipeline + Compaction

**Goal:** 实现 6 层 Agent Pipeline 和 Compaction 扩展方法
**Plans:** 2 plans

Plans:
- [x] 06-01: Implement 6-layer pipeline components
- [x] 06-02: Pipeline integration into EntryPoint + ConsoleDemo

**Details:**
- KeywordFilter: 12 blocked keywords, PromptInjectionDetector: 9 patterns
- SafetyInputFilter: PII → keyword → injection pipeline
- SafetyOutputFilter: PII redact only
- MAF CompactionProvider with PipelineCompactionStrategy (8000 token, 8 turns)
- StandardPipelineFactory assembles all 6 layers
- ConsoleDemo wires pipeline via StandardPipelineFactory.CreatePipeline

### Phase 7: Audit Logger + Saga Compensation

**Goal:** 实现 Workflow 步骤审计日志和失败补偿机制
**Plans:** 2 plans

Plans:
- [x] 07-01: Implement AuditLogger with SHA256 chain + SagaBuilder
- [x] 07-02: ConsoleDemo audit capture + Saga compensation wiring

**Details:**
- AuditLogger writes to .audit/ with SHA256 chain, VerifyChainAsync
- SagaBuilder fluent API: OnFailure → WithRetry → ExecuteAsync
- ConsoleDemo RunWorkflow captures all events via AuditTrailMiddleware
- ExecuteRefundExecutor failOnce flag for testing

### Phase 8: Business Extensibility Guide

**Goal:** 建立新增业务模块 7 步流程文档化，验证扩展性
**Plans:** 2 plans

Plans:
- [x] 08-01: Write 7-step extensibility guide document
- [x] 08-02: Create Exchange skeleton following guide

**Details:**
- BUSINESS-EXTENSIBILITY-GUIDE.md (369 lines, Chinese)
- Exchange skeleton: Workflow + 7 Executors + Messages + Skill
- All handlers throw NotImplementedException, compiles 0 errors

## Milestone Summary

**Key Decisions:**
- 30-min timeout warns without clearing workflow (Phase 5)
- 6-layer Pipeline via ChatClientBuilder middleware chain (Phase 6)
- AuditLogger uses .audit/ directory with SHA256 chain (Phase 7)
- SagaBuilder self-built framework, not MAF middleware (Phase 7)
- Extensibility guide is pure Markdown, no scaffolding script (Phase 8)

**Technical Debt Incurred:**
- ToolApproval rules deferred to v2
- Exchange Workflow business logic deferred to v2 (WF-10)
- SafetyOutput sensitive content filtering deferred to v2
- KeywordFilter config file support deferred to v2

**Build Status:** 0 errors, 0 warnings across all 5 projects
