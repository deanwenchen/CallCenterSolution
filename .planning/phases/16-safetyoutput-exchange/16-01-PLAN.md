---
phase: "16"
plan: "01"
type: execute
wave: 1
depends_on: ["15-01"]
files_modified:
  - src/CallCenter.Framework/Safety/SafetyOptions.cs
  - src/CallCenter.Framework/Safety/OutputContentFilter.cs
  - src/CallCenter.Framework/Safety/SafetyInputFilter.cs
  - src/CallCenter.Framework/Pipeline/StandardPipelineFactory.cs
  - src/CallCenter.WebApi/appsettings.json
autonomous: true
requirements: [SO-01, EX-01]
must_haves:
  truths:
    - "LLM 输出含暴力关键词 → 替换为暴力类拦截话术返回用户"
    - "LLM 输出含色情关键词 → 替换为色情类拦截话术返回用户"
    - "LLM 输出含政治敏感关键词 → 替换为政治类拦截话术返回用户"
    - "正常 LLM 输出 → PII 脱敏后正常返回，不被拦截"
    - "Exchange 骨架文件编译通过，0 错误 0 警告"
  artifacts:
    - path: src/CallCenter.Framework/Safety/OutputContentFilter.cs
      provides: "输出端按类关键词过滤，支持 GetMatchedCategory/GetFirstMatchedKeyword"
      exports: ["class OutputContentFilter"]
      min_lines: 40
    - path: src/CallCenter.Framework/Safety/SafetyOptions.cs
      provides: "输出端审核配置：BlockedOutputCategories + 3 个话术模板"
      exports: ["class SafetyOptions"]
      contains: "BlockedOutputCategories|ViolenceMessageTemplate|PornographyMessageTemplate|PoliticsMessageTemplate"
    - path: src/CallCenter.Framework/Pipeline/StandardPipelineFactory.cs
      provides: "SafetyOutputDelegatingClient 注入 SafetyOptions 和 OutputContentFilter"
      exports: ["class SafetyOutputDelegatingClient"]
      pattern: "OutputContentFilter|_contentFilter|SafetyViolationException"
  key_links:
    - from: src/CallCenter.Framework/Pipeline/StandardPipelineFactory.cs
      to: src/CallCenter.Framework/Safety/OutputContentFilter.cs
      via: "SafetyOutputDelegatingClient.GetResponseAsync 中 catch SafetyViolationException 并返回话术"
      pattern: "catch.*SafetyViolationException"
    - from: src/CallCenter.Framework/Safety/SafetyOptions.cs
      to: src/CallCenter.WebApi/appsettings.json
      via: "Options 配置绑定，Safety 节中新增 BlockedOutputCategories 和模板属性"
      pattern: "ViolenceMessageTemplate|PornographyMessageTemplate|PoliticsMessageTemplate"
---

<objective>
实现 SafetyOutput 层敏感内容拦截（SO-01），确认 Exchange 换货工作流骨架编译通过（EX-01）。

Purpose: 确保 LLM 生成的内容不含暴力/色情/政治敏感信息，在到达用户前按类别替换为友好话术。
Output: OutputContentFilter 类、扩展的 SafetyOptions、更新后的 SafetyOutputDelegatingClient、appsettings.json 新增配置、Exchange 骨架编译确认。
</objective>

<execution_context>
@D:/Claude/CallCenterSolution1/.claude/get-shit-done/workflows/execute-plan.md
@D:/Claude/CallCenterSolution1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-safetyoutput-exchange/16-CONTEXT.md
@.planning/phases/15-safety-pipeline/15-01-SUMMARY.md

<interfaces>
<!-- 现有 SafetyOutputFilter（当前仅做 PII 脱敏，需扩展） -->

From src/CallCenter.Framework/Safety/SafetyInputFilter.cs:
```csharp
public static class SafetyOutputFilter
{
    public static string ProcessOutput(string output)
    {
        return PiiRedactor.Redact(output);
    }
}

public class SafetyViolationException : Exception
{
    public string ViolationType { get; }
    public SafetyViolationException(string violationType, string message) : base(message) { }
}
```

From src/CallCenter.Framework/Pipeline/StandardPipelineFactory.cs:
```csharp
internal sealed class SafetyOutputDelegatingClient : DelegatingChatClient
{
    public SafetyOutputDelegatingClient(IChatClient inner) : base(inner) { }

    public override async Task<ChatResponse> GetResponseAsync(
        IEnumerable<ChatMessage> messages,
        ChatOptions? options = null,
        CancellationToken cancellationToken = default)
    {
        var response = await base.GetResponseAsync(messages, options, cancellationToken);
        if (response.Text is not null)
        {
            var safeOutput = SafetyOutputFilter.ProcessOutput(response.Text);
            return new ChatResponse([new ChatMessage(ChatRole.Assistant, safeOutput)])
            {
                FinishReason = response.FinishReason,
                Usage = response.Usage,
            };
        }
        return response;
    }
}
```

From src/CallCenter.Framework/Safety/SafetyOptions.cs:
```csharp
public class SafetyOptions
{
    public string[] BlockedKeywords { get; set; }
    public string BlockedMessageTemplate { get; set; }
    public bool EnableKeywordFilter { get; set; } = true;
    public bool EnableInjectionDetection { get; set; } = true;
}
```

From src/CallCenter.Framework/Safety/KeywordFilter.cs (参照模式):
```csharp
public class KeywordFilter
{
    private readonly HashSet<string> _blockedKeywords;
    public KeywordFilter(SafetyOptions options);
    public bool IsBlocked(string input);
    public string? GetBlockedKeyword(string input);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 实现 SafetyOutput 输出端敏感内容拦截（SO-01）</name>
  <files>src/CallCenter.Framework/Safety/SafetyOptions.cs, src/CallCenter.Framework/Safety/OutputContentFilter.cs, src/CallCenter.Framework/Safety/SafetyInputFilter.cs, src/CallCenter.Framework/Pipeline/StandardPipelineFactory.cs, src/CallCenter.WebApi/appsettings.json</files>
  <read_first>
    - src/CallCenter.Framework/Safety/SafetyInputFilter.cs（参考 SafetyViolationException 和 ProcessInput 模式）
    - src/CallCenter.Framework/Safety/SafetyOptions.cs（扩展输出端配置）
    - src/CallCenter.Framework/Safety/KeywordFilter.cs（参照实例+静态双 API 模式）
    - src/CallCenter.Framework/Pipeline/StandardPipelineFactory.cs（修改 SafetyOutputDelegatingClient）
    - src/CallCenter.WebApi/appsettings.json（新增输出端配置）
    - src/CallCenter.Framework/Safety/PromptInjectionDetector.cs（参照关键词匹配风格）
  </read_first>
  <action>
实现 LLM 输出端按类别关键词拦截，复用输入端一致的关键词匹配策略（D-01，不用 LLM 语义判断）。

**步骤 1：扩展 SafetyOptions（D-05）**
在 SafetyOptions 类中新增以下属性，保持与现有属性一致的 XML 注释风格：
- `string[] BlockedOutputCategories { get; set; } = ["violence", "pornography", "politics"]` — 启用的审核类别
- `string ViolenceMessageTemplate { get; set; }` — 暴力类拦截话术，默认值："抱歉，系统无法提供相关内容。如需帮助，请联系人工客服。"
- `string PornographyMessageTemplate { get; set; }` — 色情类拦截话术，默认值："抱歉，系统无法提供相关内容。如需帮助，请联系人工客服。"
- `string PoliticsMessageTemplate { get; set; }` — 政治类拦截话术，默认值："抱歉，该问题超出我的服务范围。如有其他问题，我很乐意帮助您。"

注意：默认话术应与现有 BlockedMessageTemplate 风格一致（简洁、礼貌、引导人工客服）。

**步骤 2：创建 OutputContentFilter（D-01, D-02, D-06）**
新建 `src/CallCenter.Framework/Safety/OutputContentFilter.cs`，namespace `CallCenter.Framework.Safety`。

采用与 KeywordFilter 一致的实例模式（通过 SafetyOptions 配置关键词，不硬编码）：

类结构：
- `private readonly string[] _violenceKeywords` — 暴力类关键词数组
- `private readonly string[] _pornographyKeywords` — 色情类关键词数组
- `private readonly string[] _politicsKeywords` — 政治类关键词数组
- `ctor(SafetyOptions options)` — 从 SafetyOptions 读取各类别关键词（通过 `options.GetOutputKeywords("violence")` 风格的方法，或直接从配置节读取）

注意：由于 SafetyOptions 当前是 flat 属性结构，需要在 SafetyOptions 中新增三个数组属性来存储各类别关键词：
- `string[] ViolenceKeywords { get; set; }` — 暴力类关键词，默认值见下方
- `string[] PornographyKeywords { get; set; }` — 色情类关键词，默认值见下方
- `string[] PoliticsKeywords { get; set; }` — 政治类关键词，默认值见下方

各类别默认关键词（D-02 三类覆盖，D-06 不硬编码但需默认值以便 SafetyOptions 有初始值）：
- ViolenceKeywords: ["血腥", "屠杀", "砍杀", "肢解", "炸死", "恐怖袭击", "自杀式", "人体炸弹"]
- PornographyKeywords: ["黄色", "色情", "淫秽", "裸露", "性交", "裸体", "激情", "av片"]
- PoliticsKeywords: ["台独", "藏独", "疆独", "港独", "法轮功", "民运", "六四", "天安门事件"]

方法：
- `public bool IsBlocked(string output)` — 遍历所有启用的类别关键词，检查 output 是否包含任何关键词（不区分大小写，使用 `output.Contains(keyword, StringComparison.OrdinalIgnoreCase)`）
- `public string? GetMatchedCategory(string output)` — 返回第一个匹配到的类别标识符（"violence" / "pornography" / "politics"），无匹配返回 null。按 violence → pornography → politics 顺序检查
- `public string? GetFirstMatchedKeyword(string output)` — 返回 input 中找到的第一个被拦截关键词，无匹配返回 null

**步骤 3：扩展 SafetyOutputFilter（D-03）**
修改 `src/CallCenter.Framework/Safety/SafetyInputFilter.cs` 中的 `SafetyOutputFilter` 类：

新增重载方法：
```csharp
public static string ProcessOutput(string output, SafetyOptions? options, OutputContentFilter? contentFilter)
```

逻辑：
1. 先调用 `PiiRedactor.Redact(output)` 做 PII 脱敏（保留现有行为）
2. 如果 contentFilter != null 且 options != null：
   a. 检查 options.BlockedOutputCategories 是否非空（启用输出端审核）
   b. 调用 contentFilter.IsBlocked(redacted) 检查内容
   c. 如果被拦截，调用 contentFilter.GetMatchedCategory(redacted) 获取类别
   d. 根据类别选择对应话术模板（ViolenceMessageTemplate / PornographyMessageTemplate / PoliticsMessageTemplate），抛出 `SafetyViolationException("output_content_blocked", messageTemplate)`（D-07）
3. 返回脱敏后的 output

**步骤 4：修改 SafetyOutputDelegatingClient（D-08）**
修改 `src/CallCenter.Framework/Pipeline/StandardPipelineFactory.cs` 中的 `SafetyOutputDelegatingClient`：

- 新增构造函数参数：`SafetyOptions options, OutputContentFilter? contentFilter`
- 在 GetResponseAsync 中：
  1. 调用 `SafetyOutputFilter.ProcessOutput(response.Text, _options, _contentFilter)` 替代原有的无参调用
  2. catch `SafetyViolationException` 时，返回包含话术消息的 ChatResponse（D-08 不记录审计日志，但需要正常返回给用户）：
     ```csharp
     return new ChatResponse([new ChatMessage(ChatRole.Assistant, ex.Message)])
     {
         FinishReason = response.FinishReason,
         Usage = response.Usage,
     };
     ```

**步骤 5：更新 StandardPipelineFactory.CreatePipeline 和 appsettings.json**
修改 `StandardPipelineFactory.CreatePipeline` 方法：
- 新增参数 `SafetyOptions? safetyOptions = null`
- 在创建 `SafetyOutputDelegatingClient` 时，传入 safetyOptions 和 OutputContentFilter 实例：
  ```csharp
  var contentFilter = safetyOptions != null ? new OutputContentFilter(safetyOptions) : null;
  client = new SafetyOutputDelegatingClient(client, safetyOptions ?? new SafetyOptions(), contentFilter);
  ```

在 `src/CallCenter.WebApi/appsettings.json` 的 "Safety" 节中新增输出端配置：
- `BlockedOutputCategories: ["violence", "pornography", "politics"]`
- `ViolenceKeywords: ["血腥", "屠杀", "砍杀", "肢解", "炸死", "恐怖袭击", "自杀式", "人体炸弹"]`
- `PornographyKeywords: ["黄色", "色情", "淫秽", "裸露", "性交", "裸体", "激情", "av片"]`
- `PoliticsKeywords: ["台独", "藏独", "疆独", "港独", "法轮功", "民运", "六四", "天安门事件"]`
- `ViolenceMessageTemplate: "抱歉，系统无法提供相关内容。如需帮助，请联系人工客服。"`
- `PornographyMessageTemplate: "抱歉，系统无法提供相关内容。如需帮助，请联系人工客服。"`
- `PoliticsMessageTemplate: "抱歉，该问题超出我的服务范围。如有其他问题，我很乐意帮助您。"`

注意：保持 appsettings.json 的 JSON 格式正确，新增属性放在现有 Safety 节内。
  </action>
  <verify>
    <automated>dotnet build src/CallCenter.Framework/CallCenter.Framework.csproj --no-incremental 2>&1 | grep -c "error CS" 确认 == 0</automated>
    <automated>dotnet build src/CallCenter.WebApi/CallCenter.WebApi.csproj 2>&1 | grep -c "error CS" 确认 == 0</automated>
    <automated>grep -v '^ *//' src/CallCenter.Framework/Safety/OutputContentFilter.cs | grep -c 'IsBlocked\|GetMatchedCategory\|GetFirstMatchedKeyword' 确认 >= 3</automated>
  </verify>
  <acceptance_criteria>
    <behavior>正常输出（不含敏感词）经过 SafetyOutputFilter.ProcessOutput 后仅做 PII 脱敏，原样返回</behavior>
    <behavior>输出含暴力关键词（如"血腥"）时，抛出 SafetyViolationException，ViolationType = "output_content_blocked"</behavior>
    <behavior>输出含色情关键词（如"色情"）时，抛出 SafetyViolationException，ViolationType = "output_content_blocked"</behavior>
    <behavior>输出含政治敏感关键词（如"台独"）时，抛出 SafetyViolationException，ViolationType = "output_content_blocked"</behavior>
    <behavior>SafetyOptions 包含 BlockedOutputCategories、ViolenceMessageTemplate、PornographyMessageTemplate、PoliticsMessageTemplate 属性</behavior>
    <behavior>SafetyOptions 包含 ViolenceKeywords、PornographyKeywords、PoliticsKeywords 属性</behavior>
    <behavior>OutputContentFilter ctor 接受 SafetyOptions 参数，不硬编码关键词</behavior>
    <behavior>SafetyOutputDelegatingClient 构造函数接受 SafetyOptions 和 OutputContentFilter 参数</behavior>
    <behavior>SafetyOutputDelegatingClient.GetResponseAsync 中 catch SafetyViolationException 并返回话术 ChatResponse</behavior>
    <source-assert>appsettings.json Safety 节包含 "BlockedOutputCategories"、"ViolenceKeywords"、"PornographyKeywords"、"PoliticsKeywords" 及三个 MessageTemplate 属性</source-assert>
    <source-assert>SafetyOutputFilter.ProcessOutput 新增重载接受 SafetyOptions? 和 OutputContentFilter? 参数</source-assert>
    <test-command>dotnet build src/CallCenter.Framework/CallCenter.Framework.csproj --no-incremental 编译通过，0 错误</test-command>
    <test-command>dotnet build src/CallCenter.WebApi/CallCenter.WebApi.csproj 编译通过，0 错误</test-command>
  </acceptance_criteria>
  <done>
    OutputContentFilter 类创建完成，支持按类别（violence/pornography/politics）关键词匹配；SafetyOptions 扩展输出端配置属性；SafetyOutputFilter 新增带参数的 ProcessOutput 重载；SafetyOutputDelegatingClient 捕获 SafetyViolationException 并返回类别话术；appsettings.json 新增完整输出端安全配置；所有相关项目编译通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: 确认 Exchange 换货工作流骨架编译通过（EX-01）</name>
  <files>src/CallCenter.Workflows/Exchange/ExchangeWorkflow.cs, src/CallCenter.Workflows/Exchange/ExchangeMessages.cs, src/CallCenter.Workflows/Exchange/Executors/</files>
  <read_first>
    - src/CallCenter.Workflows/Exchange/ExchangeWorkflow.cs
    - src/CallCenter.Workflows/Exchange/ExchangeMessages.cs
    - src/CallCenter.Workflows/Exchange/Executors/（7 个 Executor 文件）
  </read_first>
  <action>
确认 Exchange 骨架文件齐全且编译通过（D-09）。

Phase 16 不实现 Exchange 业务逻辑（D-10，属 v4 范围），仅验证已有骨架编译通过。

执行 `dotnet build src/CallCenter.Workflows/CallCenter.Workflows.csproj`，确认：
1. ExchangeWorkflow.cs 编译通过
2. ExchangeMessages.cs 编译通过
3. 7 个 Executor 全部编译通过：
   - GetExchangeOrderExecutor.cs
   - CheckExchangeRuleExecutor.cs
   - WaitExchangeConfirmExecutor.cs
   - ExecuteExchangeExecutor.cs
   - ExchangeRestoreCouponExecutor.cs
   - ExchangeSendNotificationExecutor.cs
   - ExchangeDeniedExecutor.cs
4. 0 错误 0 警告

如果编译失败，记录错误信息并修复（可能是项目依赖问题，不是骨架文件本身的问题）。
  </action>
  <verify>
    <automated>dotnet build src/CallCenter.Workflows/CallCenter.Workflows.csproj 2>&1 | grep -E "error|warning CS" | grep -v "warning CS1591" | head -20</automated>
  </verify>
  <acceptance_criteria>
    <test-command>dotnet build src/CallCenter.Workflows/CallCenter.Workflows.csproj 编译输出中 0 errors</test-command>
    <test-command>dotnet build src/CallCenter.Workflows/CallCenter.Workflows.csproj 编译输出中 0 非文档类 warnings（CS1591 文档警告可忽略）</test-command>
    <source-assert>src/CallCenter.Workflows/Exchange/Executors/ 目录下存在 7 个 .cs 文件</source-assert>
    <behavior>ExchangeWorkflow.cs、ExchangeMessages.cs、7 个 Executor 文件均存在且内容完整</behavior>
  </acceptance_criteria>
  <done>
    Exchange 换货工作流骨架确认：Workflow 定义、消息类型、7 个 Executor 骨架文件齐全，CallCenter.Workflows 项目编译通过，0 错误 0 警告。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM 响应 → SafetyOutputFilter | LLM 可能输出暴力/色情/政治敏感内容，需关键词拦截 |
| appsettings.json → SafetyOptions → OutputContentFilter | 配置文件被篡改可能导致审核类别被禁用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | LLM 响应 | mitigate | SafetyOutputFilter.ProcessOutput 对所有输出做关键词匹配拦截，3 类关键词全覆盖（violence/pornography/politics），任一命中抛出 SafetyViolationException |
| T-16-02 | Tampering | appsettings.json Safety 节 | mitigate | BlockedOutputCategories 默认启用全部 3 类；删除类别需在配置文件中显式操作，非运行时注入 |
| T-16-03 | Information Disclosure | SafetyViolationException message | accept | 话术模板使用通用友好提示语，不暴露拦截规则或关键词细节 |
| T-16-04 | Elevation of Privilege | OutputContentFilter 关键词数组 | accept | 关键词列表仅用于内容匹配，不涉及权限控制，低价值攻击目标 |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS]（本阶段无包安装任务） |
</threat_model>

<verification>
## Overall Phase Verification

1. `dotnet build` 全解决方案编译通过，0 错误 0 警告
2. SafetyOutput 拦截验证：构造含敏感词的字符串传入 SafetyOutputFilter.ProcessOutput，确认抛出 SafetyViolationException 且 ViolationType = "output_content_blocked"
3. 正常输出验证：构造不含敏感词的字符串传入，确认正常返回 PII 脱敏后的内容
4. Exchange 骨架验证：`dotnet build src/CallCenter.Workflows/CallCenter.Workflows.csproj` 确认 7 个 Executor + Workflow + Messages 编译通过
5. appsettings.json 验证：Safety 节包含输出端全部配置属性
</verification>

<success_criteria>
- [ ] OutputContentFilter.cs 已创建，包含 IsBlocked/GetMatchedCategory/GetFirstMatchedKeyword 方法
- [ ] SafetyOptions.cs 新增 BlockedOutputCategories、3 个话术模板、3 个关键词数组属性
- [ ] SafetyOutputFilter.ProcessOutput 新增重载支持 options 和 contentFilter 参数
- [ ] SafetyOutputDelegatingClient 注入 SafetyOptions 和 OutputContentFilter，catch SafetyViolationException 返回话术
- [ ] StandardPipelineFactory.CreatePipeline 传入 safetyOptions
- [ ] appsettings.json Safety 节包含完整输出端安全配置
- [ ] CallCenter.Framework 项目编译通过
- [ ] CallCenter.WebApi 项目编译通过
- [ ] CallCenter.Workflows 项目编译通过（Exchange 骨架确认）
- [ ] 3 类关键词覆盖：暴力/色情/政治
</success_criteria>

<output>
Create `.planning/phases/16-safetyoutput-exchange/16-01-SUMMARY.md` when done
</output>
