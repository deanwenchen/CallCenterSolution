# Phase 17: Session 持久化存储 — Plan 17-01

**Goal:** ISessionStore 接口 + RedisSessionStore 实现 + DI 配置切换
**Date:** 2026-06-05
**Status:** Ready to execute

## Requirements Traceability

| Requirement | Status | Task |
|-------------|--------|------|
| SS-01: ISessionStore 接口 5+ 方法 | ✅ Already exists | — |
| SS-02: InMemorySessionStore 实现 | ✅ Already exists | — |
| SS-03: RedisSessionStore 实现 | ✅ Already exists | Task 1 |
| SS-04: AddSessionStore DI 扩展 | ✅ Already exists | — |
| SS-05: 消费者改用 ISessionStore | ⚠️ Partial | Task 2 |
| SS-06: appsettings.json SessionStore 配置 | ❌ Missing | Task 3 |
| SS-07: 编译通过 + 验证 | ❌ Pending | Task 4 |

## Tasks

### Task 1: 验证 RedisSessionStore 实现完整性
- **File:** `src/CallCenter.Framework/Session/RedisSessionStore.cs`
- **Status:** ✅ Already implemented using Leo.Data.Redis `RedisHelper.Get<T>`, `SetAsync<T>`, `GetValueAsync`, extension `RemoveAsync`, `GetClientAsync` + `SearchKeysAsync`/`RemoveAllAsync`
- **Action:** No changes needed — implementation already matches decisions D-04, D-05, D-06

### Task 2: 修复 CallCenterService DI 构造函数 — 改用 ISessionStore
- **File:** `src/CallCenter.AgentHost/CallCenterService.Core.cs:113`
- **Change:** `provider.GetRequiredService<InMemorySessionStore>()` → `provider.GetRequiredService<ISessionStore>()`
- **Reason:** DI 构造函数应解析接口而非具体实现，让 `AddSessionStore` 的配置生效

### Task 3: 添加 appsettings.json SessionStore 配置
- **File:** `src/CallCenter.WebApi/appsettings.json`
- **Action:** 新增 `"SessionStore": { "Provider": "memory" }` 配置段（默认内存模式，功能不变）

### Task 4: 编译验证
- **Action:** `dotnet build` 全解决方案编译，确认无错误

## Execution Order

1. Task 2 — 修复 DI 构造函数（1 line change）
2. Task 3 — 添加配置（1 JSON block）
3. Task 4 — 编译验证
