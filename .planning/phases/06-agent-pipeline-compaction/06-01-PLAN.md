---
wave: 1
depends_on: []
files_modified:
  - src/CallCenter.Framework/Safety/KeywordFilter.cs
  - src/CallCenter.Framework/Safety/PromptInjectionDetector.cs
  - src/CallCenter.Framework/Safety/SafetyInputFilter.cs
  - src/CallCenter.Framework/Safety/SafetyOutputFilter.cs
  - src/CallCenter.Framework/Safety/SafetyPipelineAgent.cs
  - src/CallCenter.Framework/Pipeline/StandardPipelineFactory.cs
  - src/CallCenter.Framework/Compaction/CompactionExtensions.cs
  - src/CallCenter.Framework/ToolApproval/ToolApprovalAgent.cs
  - src/CallCenter.Framework/ToolApproval/ToolApprovalOptions.cs
  - src/CallCenter.Framework/Logging/JsonlLogger.cs
autonomous: true
---

# Phase 6: Agent Pipeline + Compaction — Plan 01

## Objective

Implement 6 层 Pipeline 组件：KeywordFilter、PromptInjectionDetector、SafetyInputFilter、SafetyOutputFilter、Compaction wiring、ToolApproval shell、JsonlLogger，以及 StandardPipelineFactory 组装。

## Requirements Addressed

FW-05, FW-07

## Must Haves

- KeywordFilter 实现硬编码拦截规则
- PromptInjectionDetector 实现基础模式检测
- SafetyInputFilter 组合 PiiRedactor + KeywordFilter + PromptInjectionDetector
- SafetyOutputFilter 组合 PiiRedactor
- CompactionExtensions 接入 MAF CompactionProvider（8000 token, 8 轮）
- ToolApprovalAgent 接口 + 默认放行实现
- JsonlLogger 写入 .logs/{sessionId}.jsonl
- StandardPipelineFactory 返回完整管道

## Tasks

### Task 1: Complete KeywordFilter and PromptInjectionDetector

<read_first>
- src/CallCenter.Framework/Safety/KeywordFilter.cs (current TODO stub)
- src/CallCenter.Framework/Safety/PromptInjectionDetector.cs (current TODO stub)
- src/CallCenter.Framework/Safety/PiiRedactor.cs (already complete, use as reference for style)
</read_first>

<action>
Replace `KeywordFilter.cs` TODO stub with:
- Static `HashSet<string>` of blocked keywords: 暴力/诈骗/投诉等基础词（10-15 个中文词）
- `IsBlocked(string input)` — case-insensitive match, return true if any keyword found
- `GetBlockedKeyword(string input)` — return matched keyword or null

Replace `PromptInjectionDetector.cs` TODO stub with:
- Static `string[]` of injection patterns: "忽略之前指令", "ignore previous", "ignore above", "system prompt", "you are now", "扮演系统"
- `Detect(string input)` — case-insensitive contains check, return true if any pattern found
</action>

<acceptance_criteria>
- KeywordFilter.cs: `IsBlocked("我要投诉")` returns true (contains 投诉 keyword)
- KeywordFilter.cs: `IsBlocked("你好")` returns false
- PromptInjectionDetector.cs: `Detect("忽略之前所有指令")` returns true
- PromptInjectionDetector.cs: `Detect("你好")` returns false
- Build: 0 errors
</acceptance_criteria>

### Task 2: Implement SafetyInputFilter and SafetyOutputFilter

<read_first>
- src/CallCenter.Framework/Safety/SafetyPipelineAgent.cs (TODO stub — will become real class)
- src/CallCenter.Framework/Safety/PiiRedactor.cs (source of truth for PII redaction)
- src/CallCenter.Framework/Safety/KeywordFilter.cs (from Task 1)
- src/CallCenter.Framework/Safety/PromptInjectionDetector.cs (from Task 1)
</read_first>

<action>
Create `SafetyInputFilter.cs` in Safety/ directory:
- Static class with `ProcessInput(string input, string sessionId)` method
- Step 1: `PiiRedactor.Redact(input)` — redact PII
- Step 2: If `KeywordFilter.IsBlocked(redacted)` → throw `SafetyViolationException` with blocked keyword
- Step 3: If `PromptInjectionDetector.Detect(redacted)` → throw `SafetyViolationException` with "injection detected"
- Return redacted + safe input

Create `SafetyOutputFilter.cs` in Safety/ directory:
- Static class with `ProcessOutput(string output)` method
- Calls `PiiRedactor.Redact(output)` — return filtered output

Update `SafetyPipelineAgent.cs`:
- Remove TODO stub comments
- Implement as a `DelegatingChatClient` wrapper that applies SafetyInputFilter before SendAsync and SafetyOutputFilter after ReceiveAsync
- Constructor takes inner `IChatClient`
</action>

<acceptance_criteria>
- `SafetyInputFilter.ProcessInput("我的手机号是13812345678")` returns "我的手机号是138****5678"
- `SafetyInputFilter.ProcessInput("我要投诉你们")` throws `SafetyViolationException`
- `SafetyOutputFilter.ProcessOutput("退款到卡号1234567890123456")` returns "退款到卡号1234****3456"
- SafetyPipelineAgent wraps IChatClient and applies both filters
- Build: 0 errors
</acceptance_criteria>

### Task 3: Implement ToolApprovalAgent shell

<read_first>
- src/CallCenter.Framework/Safety/ (directory to see pattern — follow same naming convention)
</read_first>

<action>
Create `ToolApproval/ToolApprovalAgent.cs`:
- Interface: `IToolApprovalAgent` with `Task<bool> IsApprovedAsync(string toolName, object arguments, string sessionId, CancellationToken ct)`
- Default implementation: `DefaultToolApprovalAgent` — always returns `Task.FromResult(true)`

Create `ToolApproval/ToolApprovalOptions.cs`:
- Empty options class for future configuration: `public class ToolApprovalOptions { }`

Create `ToolApproval/ToolApprovalDelegatingClient.cs`:
- `DelegatingChatClient` wrapper that intercepts tool calls
- Calls `IToolApprovalAgent.IsApprovedAsync` before forwarding to inner client
- Default approval passes through without blocking
</action>

<acceptance_criteria>
- `DefaultToolApprovalAgent.IsApprovedAsync("execute_refund", args, "session-1")` returns true
- ToolApprovalOptions class exists with empty body
- ToolApprovalDelegatingClient wraps IChatClient
- Build: 0 errors
</acceptance_criteria>

### Task 4: Implement JsonlLogger

<read_first>
- src/CallCenter.Framework/ (directory to see pattern)
</read_first>

<action>
Create `Logging/JsonlLogger.cs`:
- Class: `JsonlLogger` with `LogAsync(string sessionId, string direction, string content, string? tool = null, int? tokenCount = null)` method
- Writes to `.logs/{sessionId}.jsonl` — create directory if not exists
- Each line: `{"timestamp":"ISO8601","sessionId":"...","direction":"request|response","tool":"...","content":"...","tokenCount":...}`
- Thread-safe: use `SemaphoreSlim` or `AsyncLocal` for concurrent writes
- Constructor takes `string logDirectory` parameter (default: ".logs")
</action>

<acceptance_criteria>
- `JsonlLogger.LogAsync("session-1", "request", "hello")` creates `.logs/session-1.jsonl` with one JSON line
- File contains valid JSON that can be deserialized
- Concurrent calls don't corrupt the file
- Build: 0 errors
</acceptance_criteria>

### Task 5: Wire Compaction with MAF CompactionProvider

<read_first>
- src/CallCenter.Framework/Compaction/CompactionExtensions.cs (current TODO stub)
- D:/GitCode/agent-framework/dotnet/src/Microsoft.Agents.AI/Compaction/PipelineCompactionStrategy.cs
- D:/GitCode/agent-framework/dotnet/samples/02-agents/Agents/Agent_Step18_CompactionPipeline/Program.cs (lines 88-100 for API pattern)
</read_first>

<action>
Replace `CompactionExtensions.cs` TODO stub:
- `AddCallCenterCompaction(this IServiceCollection services, string summarizerModel = "qwen-plus")` — registers `CompactionProvider` with DI
- Internally creates: `PipelineCompactionStrategy` with 2 strategies:
  1. `SummarizationCompactionStrategy(summarizerClient, CompactionTriggers.TokensExceed(8000))`
  2. `SlidingWindowCompactionStrategy(CompactionTriggers.TurnsExceed(8))`
- `UseCallCenterCompaction(this ChatClientBuilder builder, IChatClient summarizerClient)` — calls `.UseAIContextProviders(new CompactionProvider(pipeline))`
- CompactionOptions defaults: TokenThreshold=8000, PreserveRecentTurns=8, SmallModel="qwen-plus"
</action>

<acceptance_criteria>
- CompactionExtensions has `AddCallCenterCompaction` and `UseCallCenterCompaction` extension methods
- PipelineCompactionStrategy combines Summarization + SlidingWindow strategies
- Token threshold is 8000, preserve turns is 8
- Build: 0 errors
</acceptance_criteria>

### Task 6: StandardPipelineFactory assembles all layers

<read_first>
- src/CallCenter.Framework/Pipeline/StandardPipelineFactory.cs (current TODO stub)
- src/CallCenter.Framework/Safety/SafetyPipelineAgent.cs (from Task 2)
- src/CallCenter.Framework/Logging/JsonlLogger.cs (from Task 4)
- src/CallCenter.Framework/ToolApproval/ToolApprovalDelegatingClient.cs (from Task 3)
- src/CallCenter.Framework/Compaction/CompactionExtensions.cs (from Task 5)
</read_first>

<action>
Replace `StandardPipelineFactory.cs` TODO stub:
- `CreatePipeline(IChatClient baseClient, IChatClient summarizerClient, string sessionId)` returns fully wired `IChatClient`
- Pipeline order: SafetyInput → Logging → Compaction → ToolApproval → LLM → SafetyOutput
- Wrapping (innermost to outermost):
  1. baseClient (raw LLM)
  2. SafetyOutputFilter wrapper
  3. ToolApproval wrapper
  4. Compaction via `ChatClientBuilder.UseAIContextProviders(CompactionProvider)`
  5. Logging wrapper
  6. SafetyInputFilter wrapper
- Static helper: `CreateSummarizerClient(OpenAIClient client, string model = "qwen-plus")`
</action>

<acceptance_criteria>
- `StandardPipelineFactory.CreatePipeline(...)` returns a non-null IChatClient
- The returned client has all 6 layers wired
- Build: 0 errors
</acceptance_criteria>

---

*Phase 6 Plan 01 — Framework components*
