---
wave: 2
depends_on: [06-01]
files_modified:
  - src/CallCenter.AgentHost/EntryPoint.cs
  - src/CallCenter.ConsoleDemo/Program.cs
  - src/CallCenter.Framework/ServiceCollectionExtensions.cs
autonomous: true
---

# Phase 6: Agent Pipeline + Compaction — Plan 02

## Objective

Wire pipeline into EntryPoint and ConsoleDemo. EntryPoint receives piped IChatClient; ConsoleDemo assembles the pipeline.

## Requirements Addressed

FW-05

## Must Haves

- EntryPoint constructor accepts IChatClient (already piped) — no code change needed, just doc update
- ConsoleDemo assembles pipeline using StandardPipelineFactory
- ServiceCollectionExtensions registers pipeline components
- End-to-end: user input → SafetyInput → Logging → Compaction → ToolApproval → LLM → SafetyOutput → console

## Tasks

### Task 1: Wire pipeline into ConsoleDemo

<read_first>
- src/CallCenter.ConsoleDemo/Program.cs (current: creates raw IChatClient, passes to EntryPoint)
- src/CallCenter.Framework/Pipeline/StandardPipelineFactory.cs (from Plan 01)
- src/CallCenter.AgentHost/EntryPoint.cs (accepts IChatClient — already correct signature per D-07)
</read_first>

<action>
In `Program.cs`, after creating the raw IChatClient (line 21-25):
1. Create summarizer client: `IChatClient summarizerClient = ...` (use same OpenAIClient with "qwen-plus" model)
2. Build pipeline: `IChatClient pipeline = StandardPipelineFactory.CreatePipeline(chatClient, summarizerClient, sessionId)`
3. Pass pipeline to EntryPoint: `var entryPoint = new EntryPoint(pipeline, sessionStore, skillsProvider)`
4. Also create a separate `JsonlLogger` instance and wire it via `StandardPipelineFactory` (or pass sessionId to factory)

The EntryPoint signature does NOT change — it already accepts `IChatClient`. The pipeline is assembled in Program.cs before passing it in.
</action>

<acceptance_criteria>
- Program.cs creates summarizer IChatClient (qwen-plus model)
- Program.cs calls StandardPipelineFactory.CreatePipeline with raw client + summarizer + sessionId
- EntryPoint receives piped IChatClient (not raw)
- Build: 0 errors
</acceptance_criteria>

### Task 2: Register pipeline components in ServiceCollectionExtensions

<read_first>
- src/CallCenter.Framework/ServiceCollectionExtensions.cs (current: only registers EventBus + SessionStore)
- src/CallCenter.Framework/Compaction/CompactionExtensions.cs (from Plan 01)
- src/CallCenter.Framework/Logging/JsonlLogger.cs (from Plan 01)
</read_first>

<action>
In `ServiceCollectionExtensions.cs`, add to `AddCallCenter` method:
- `services.AddSingleton<JsonlLogger>()` — default log directory
- Call `services.AddCallCenterCompaction()` if summarizer client is available (or provide overload)

This ensures DI registration of all pipeline components for future use (Phase 7+).
</action>

<acceptance_criteria>
- ServiceCollectionExtensions.AddCallCenter registers JsonlLogger
- Build: 0 errors
</acceptance_criteria>

---

*Phase 6 Plan 02 — Pipeline integration into EntryPoint + ConsoleDemo*
