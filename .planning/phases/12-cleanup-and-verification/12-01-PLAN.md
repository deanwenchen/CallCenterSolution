---
phase: 12-cleanup-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []

must_haves:
  truths:
    - "ServiceCollectionExtensions.cs 中旧的 AddCallCenter 方法被删除"
    - "全解决方案编译通过，0 错误"
    - "所有源文件无未使用的 using 引用"
  artifacts:
    - path: "src/CallCenter.Framework/ServiceCollectionExtensions.cs"
      provides: "旧 AddCallCenter 方法删除"
    - path: "src/CallCenter.AgentHost/*.cs"
      provides: "清理未使用的 using 指令"
  key_links:
    - from: "src/CallCenter.AgentHost/Extensions.cs"
      to: "src/CallCenter.AgentHost/CallCenterService.Core.cs"
      via: "AddCallCenter 调用"
      pattern: "services\\.AddCallCenter\\("
---

<objective>
清理旧代码：删除 Framework 中废弃的 AddCallCenter 方法，清理各文件未使用的 using 指令。

Purpose: v2.0 框架提取的最后清理步骤，确保代码库无死代码。
Output: 干净的 Framework + AgentHost 源码
</objective>

<execution_context>
@D:/Claude/CallCenterSolution1/.claude/get-shit-done/workflows/execute-plan.md
@D:/Claude/CallCenterSolution1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-cleanup-and-verification/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 删除 ServiceCollectionExtensions.cs 中的旧 AddCallCenter 方法</name>
  <read_first>
    - src/CallCenter.Framework/ServiceCollectionExtensions.cs — 删除目标
    - src/CallCenter.AgentHost/Extensions.cs — 新 AddCallCenter（替代者）
    - src/CallCenter.AgentHost/CallCenterService.Core.cs — 确认调用的是 Extensions.cs 版本（有 options 参数）
  </read_first>
  <acceptance_criteria>
    <source_assertion>src/CallCenter.Framework/ServiceCollectionExtensions.cs 不存在（文件删除）或文件中不再包含 AddCallCenter 方法</source_assertion>
    <cli>dotnet build 编译通过，无新增错误</cli>
    <behavior>Core.cs 的 services.AddCallCenter(_options) 仍正常工作（调用 Extensions.cs 版本）</behavior>
  </acceptance_criteria>
  <action>
    删除 src/CallCenter.Framework/ServiceCollectionExtensions.cs 文件。

    原因分析：
    - 该文件定义 `CallCenter.Framework.ServiceCollectionExtensions.AddCallCenter(this IServiceCollection)` 无 options 参数
    - Core.cs 调用 `services.AddCallCenter(_options)` 有 options 参数，匹配 Extensions.cs 版本
    - Framework 版本无任何调用者（grep 确认）
    - Extensions.cs 版本功能更完整（IChatClient、Pipeline、SessionStore、AuditLogger、EventBus、Workflow、Mock Services 等）

    完成后编译验证：dotnet build
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -c "error"</automated>
  </verify>
  <done>ServiceCollectionExtensions.cs 删除；编译通过；AddCallCenter 调用正常</done>
</task>

<task type="auto">
  <name>Task 2: 清理所有源文件未使用的 using 指令</name>
  <read_first>
    - src/CallCenter.AgentHost/CallCenterService.Core.cs — 18 个 using，大量未使用
    - src/CallCenter.AgentHost/CallCenterService.Execution.cs — 7 个 using
    - src/CallCenter.AgentHost/CallCenterService.Intent.cs — 4 个 using
    - src/CallCenter.AgentHost/CallCenterService.Interaction.cs — 5 个 using
    - src/CallCenter.AgentHost/CallCenterService.Routing.cs — 5 个 using
    - src/CallCenter.AgentHost/EntryPoint.cs — 8 个 using
    - src/CallCenter.AgentHost/Extensions.cs — 11 个 using
    - src/CallCenter.AgentHost/AIAgentFactory.cs — 3 个 using
    - src/CallCenter.AgentHost/IntentRegistry.cs — 3 个 using
  </read_first>
  <acceptance_criteria>
    <cli>dotnet build 编译通过，无新增错误</cli>
    <behavior>每个文件的 using 指令仅包含实际引用的命名空间</behavior>
    <source_assertion>Core.cs 删除：System.ClientModel, System.Text.Json, Microsoft.Agents.AI, OpenAI 等未使用的 using</source_assertion>
  </acceptance_criteria>
  <action>
    对每个源文件，逐个检查 using 指令是否被实际使用。删除未引用的 using。

    注意：
    - 使用 `dotnet build` 后确认编译通过
    - 不要删除被间接引用的命名空间（如通过 global usings 或类型推断使用的）
    - 不确定的 using 保留，不要过度清理
    - 优先清理明显未使用的：System.ClientModel, OpenAI, System.Text.Json 等在已迁移到 DI 模式的文件中
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -c "error"</automated>
  </verify>
  <done>所有文件无未使用的 using；编译通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 本 Phase 仅删除死代码和清理 using，不引入新的运行时行为 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-SC | Tampering | npm/pip/cargo installs | N/A | No package manager installs — pure C# cleanup |
</threat_model>

<verification>
## 整体验证

1. 编译全解决方案：`dotnet build` 无新增错误或警告
2. 源码断言：ServiceCollectionExtensions.cs 不存在或不含 AddCallCenter
3. 源码断言：各文件 using 指令数量减少，无冗余

</verification>

<success_criteria>
- [ ] ServiceCollectionExtensions.cs 删除或不含 AddCallCenter
- [ ] 全解决方案编译通过，无新增错误
- [ ] 各源文件无未使用的 using 指令
- [ ] CallCenterService 功能不受影响
</success_criteria>

<output>
Create `.planning/phases/12-cleanup-and-verification/12-01-SUMMARY.md` when done
</output>
