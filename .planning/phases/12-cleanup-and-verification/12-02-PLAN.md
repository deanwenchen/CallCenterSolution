---
phase: 12-cleanup-and-verification
plan: 02
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []

must_haves:
  truths:
    - "ProcessAsync 对非业务意图返回问候语（不启动工作流）"
    - "ProcessAsync 对退款意图返回工作流执行结果"
    - "EventBus 订阅在退款完成时输出格式化的事件消息"
    - "全解决方案编译通过，0 错误"
  artifacts:
    - path: "src/CallCenter.AgentHost/CallCenterService.Intent.cs"
      provides: "ProcessAsync 入口，switch ProcessResult dispatch"
    - path: "src/CallCenter.AgentHost/CallCenterService.Execution.cs"
      provides: "RunWorkflowAsync / ResumeWorkflowAsync 工作流执行"
    - path: "src/CallCenter.AgentHost/CallCenterService.Core.cs"
      provides: "EventBus 订阅（RefundCompletedEvent）"
  key_links:
    - from: "Program.cs"
      to: "CallCenterService.Intent.cs"
      via: "svc.ProcessAsync"
      pattern: "ProcessAsync\\("
---

<objective>
端到端验证：确认重构后的 CallCenterService 行为与重构前一致。

Purpose: 验证 v2.0 框架提取没有破坏现有功能。
Output: VERIFICATION.md 记录 4 个 E2E 场景的验证结果
</objective>

<execution_context>
@D:/Claude/CallCenterSolution1/.claude/get-shit-done/workflows/execute-plan.md
@D:/Claude/CallCenterSolution1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-cleanup-and-verification/12-CONTEXT.md
@.planning/phases/11-execution-entry/11-01-SUMMARY.md
@.planning/phases/10-callcenter-service-skeleton/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 编译验证 + 源码断言</name>
  <read_first>
    - src/CallCenter.ConsoleDemo/Program.cs — 当前入口（18 行）
    - src/CallCenter.AgentHost/CallCenterService.Intent.cs — ProcessAsync 入口
    - src/CallCenter.AgentHost/CallCenterService.Core.cs — EventBus 订阅
  </read_first>
  <acceptance_criteria>
    <cli>dotnet build 全解决方案编译通过，0 错误，0 新增警告</cli>
    <source_assertion>Program.cs 行数 ≤ 35</source_assertion>
    <source_assertion>Program.cs 不包含 RunWorkflow/ResumeWorkflow/HandleRequestAsync 静态方法</source_assertion>
    <source_assertion>Program.cs 包含 new CallCenterService()</source_assertion>
    <source_assertion>Program.cs 包含 svc.ProcessAsync(sessionId, userMessage)</source_assertion>
    <source_assertion>CallCenterService.Core.cs 包含 eventBus.Subscribe&lt;RefundCompletedEvent&gt;（两个构造函数各一个）</source_assertion>
    <source_assertion>CallCenterService.Execution.cs 包含 DriveLoopAsync + HandleEventAsync（9 种事件类型）</source_assertion>
  </acceptance_criteria>
  <action>
    1. 运行 dotnet build 全解决方案编译
    2. 对 Program.cs 和 Core.cs 执行源码断言检查
    3. 记录验证结果
  </action>
  <verify>
    <automated>dotnet build 2>&1 | grep -c "error"</automated>
  </verify>
  <done>编译通过；所有源码断言验证通过</done>
</task>

<task type="human">
  <name>Task 2: E2E 冒烟测试（4 个场景）</name>
  <read_first>
    - src/CallCenter.AgentHost/CallCenterService.Intent.cs — ProcessAsync dispatch 逻辑
    - src/CallCenter.AgentHost/CallCenterService.Routing.cs — ResolveWorkflow 路由逻辑
    - .planning/phases/12-cleanup-and-verification/12-CONTEXT.md — E2E 测试场景定义
  </read_first>
  <acceptance_criteria>
    <behavior>T1 — 业务意图（退款）: 输入 "我要退款，订单A001" → 进入退款流程 → 确认 → 退款完成 → 输出 "[EVENT] 退款完成: 订单A001, 金额 ¥XX.XX"</behavior>
    <behavior>T2 — 规则拒绝: 输入 "我要退款，订单A002"（定制商品）→ 返回规则拒绝消息</behavior>
    <behavior>T3 — 非业务意图: 输入 "你好" → 返回问候语，不启动工作流</behavior>
    <behavior>T4 — 追问缺失参数: 输入 "我要退款"（无订单号）→ 追问订单号 → 用户提供 → 继续流程</behavior>
  </acceptance_criteria>
  <action>
    运行 dotnet run --project src/CallCenter.ConsoleDemo/CallCenter.ConsoleDemo.csproj

    依次执行 4 个测试场景：

    T1: 输入 "我要退款，订单A001"
      预期: 显示订单信息 → 确认退款 → 显示退款结果 → 显示 [EVENT] 退款完成

    T2: 输入 "我要退款，订单A002"
      预期: 返回规则拒绝（定制商品不可退款）

    T3: 输入 "你好"
      预期: 返回问候语，不启动任何工作流

    T4: 输入 "我要退款"
      预期: 追问订单号 → 输入订单号 → 继续退款流程

    记录每个场景的实际输出到 VERIFICATION.md
  </action>
  <verify>
    <manual>人工观察 4 个场景的控制台输出是否符合预期</manual>
  </verify>
  <done>4 个 E2E 场景全部验证通过（或记录失败原因）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Console In → ProcessAsync | 用户输入直接进入服务，无额外过滤（Demo 环境可接受） |
| LLM API → Intent Recognition | 依赖外部 DashScope API，可能返回意外结果 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-SC | Tampering | npm/pip/cargo installs | N/A | No package manager installs — pure verification |
</threat_model>

<verification>
## 整体验证

1. 编译：dotnet build 无错误
2. E2E T1: 退款流程端到端跑通
3. E2E T2: 规则拒绝正确返回
4. E2E T3: 非业务意图返回问候语
5. E2E T4: 追问缺失参数流程正确

</verification>

<success_criteria>
- [ ] 全解决方案编译通过，0 错误
- [ ] T1: 退款流程端到端跑通，EventBus 事件输出正确
- [ ] T2: 规则拒绝正确返回
- [ ] T3: 非业务意图返回问候语
- [ ] T4: 追问缺失参数流程正确
</success_criteria>

<output>
Create `.planning/phases/12-cleanup-and-verification/12-02-SUMMARY.md` when done
</output>
