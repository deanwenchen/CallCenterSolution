---
wave: 1
depends_on: []
files_modified:
  - src/CallCenter.AgentHost/Skills/RefundSkill.cs
requirements: [SK-01, SK-02]
autonomous: true
---

# 计划 02：RefundSkill — AgentClassSkill 完整实现

## 目标

实现 RefundSkill 作为 AgentClassSkill<RefundSkill>，包含 Frontmatter、Instructions、以及 2 个脚本（get_recent_orders、execute_refund），通过 DI 调用 MCP 客户端接口。

## 任务

### 任务 2.1：创建 RefundSkill.cs

<read_first>
- .planning/phases/02-agenthost-intent-router/02-CONTEXT.md（D-58~D-64：Skill 完整注册）
- Prd.md Section 4.1（RefundSkill AgentClassSkill 定义，第 251-296 行）
- openspec/changes/refund-workflow-demo/specs/refund-workflow/spec.md（AgentClassSkill 定义需求，第 131-144 行）
- openspec/changes/refund-workflow-demo/design.md（第 11 节：RefundSkill 完整设计）
- MAF 参考：AgentClassSkill.cs（AgentClassSkill<TSelf> 基类）
- MAF 参考：AgentSkillFrontmatter.cs（Frontmatter 构造函数）
- MAF 参考：AgentSkillScriptAttribute.cs（[AgentSkillScript] 属性）
- src/CallCenter.Shared/Mcp/IOrderMcpClient.cs（Phase 1 完成后）
- src/CallCenter.Shared/Mcp/IFinanceMcpClient.cs（Phase 1 完成后）
- src/CallCenter.Shared/Models/OrderInfo.cs（Phase 1 完成后）
- src/CallCenter.Shared/Models/RefundResult.cs（Phase 1 完成后）
</read_first>

<acceptance_criteria>
- src/CallCenter.AgentHost/Skills/RefundSkill.cs 存在
- 命名空间：CallCenter.AgentHost.Skills
- Class RefundSkill : AgentClassSkill<RefundSkill>
- Frontmatter 属性：
  - new AgentSkillFrontmatter("refund", "处理用户退款请求。当用户要求退款、退货、取消订单时使用。支持查询订单、校验退款资格、计算退款金额、执行退款。")
- Instructions 属性（protected override string）：
  - 多行字符串，解释退款流程步骤：
    1. 获取订单号（如果用户未提供，使用 get_recent_orders 脚本获取最近订单）
    2. 系统将自动处理退款流程，包括资格校验、金额计算、用户确认
    3. 退款完成后通知用户结果
- 脚本：[AgentSkillScript("get_recent_orders")]
  - static async Task<string> GetRecentOrders(string? userId, IServiceProvider sp)
  - 从 sp 解析 IOrderMcpClient
  - 调用 client.GetRecentOrdersAsync(userId ?? "U100")
  - 返回 JsonSerializer.Serialize(orders)
- 脚本：[AgentSkillScript("execute_refund")]
  - static async Task<string> ExecuteRefund(string orderId, decimal amount, IServiceProvider sp)
  - 从 sp 解析 IFinanceMcpClient
  - 调用 client.RefundAsync(orderId, amount)
  - 返回 JsonSerializer.Serialize(result)
- 必要的 using 语句：Microsoft.Agents.AI、Microsoft.Agents.AI.Skills、CallCenter.Shared.Mcp、CallCenter.Shared.Models、System.Text.Json、System.ComponentModel
- 如 AgentClassSkill 需要，使用 System.Diagnostics.CodeAnalysis.Experimental 属性
</acceptance_criteria>

<action>
按照 Prd.md Section 4.1 精确创建 RefundSkill.cs。关键点：

1. AgentClassSkill<TSelf> 要求 TSelf 类型参数为同一个类
2. Frontmatter 是属性重写，用 new AgentSkillFrontmatter(name, description) 初始化
3. Instructions 是 protected override string 属性
4. Scripts 是静态方法，带 [AgentSkillScript("name")] 属性
5. 脚本方法接收 IServiceProvider 用于 DI 解析 MCP 客户端
6. 使用 JsonSerializer.Serialize() 返回 JSON 字符串
7. userId 为 null 时默认为 "U100"（匹配 MockOrderService 的 userId）

注意：AgentClassSkill 及相关类型可能有 [Experimental] 属性。如果出现编译警告，添加适当的 #pragma warning disable 或 [Experimental] 属性到类上。
</action>

### 任务 2.2：验证 RefundSkill 编译

<acceptance_criteria>
- `dotnet build src/CallCenter.AgentHost/CallCenter.AgentHost.csproj` 成功，0 错误
- RefundSkill 正确编译
- AgentClassSkill 基类可找到（MAF.AI 引用）
- [AgentSkillScript] 属性可找到
</acceptance_criteria>

<action>
对 AgentHost 项目执行 dotnet build。修复任何编译错误。可能需要检查 MAF.AI 项目中 AgentClassSkill 和 AgentSkillScriptAttribute 的确切命名空间。
</action>
