---
wave: 1
depends_on: []
files_modified:
  - src/CallCenter.AgentHost/EntryPoint.cs
requirements: [IR-01, IR-02, IR-03]
autonomous: true
---

# 计划 01：EntryPoint — LLM 意图识别 + 活跃工作流管理

## 目标

创建 EntryPoint 类，使用 DashScope Qwen 进行基于 LLM 的意图识别、activeWorkflow 会话管理、以及决定新启动还是恢复工作流的 ProcessAsync 方法。

## 任务

### 任务 1.1：创建 EntryPoint.cs

<read_first>
- .planning/phases/02-agenthost-intent-router/02-CONTEXT.md（D-43~D-68：所有 Phase 2 决策）
- .planning/phases/01-foundation/01-CONTEXT.md（D-07~D-09：DI，D-33~D-36：EventBus/SessionStore）
- Prd.md Section 5.1（入口点：检查 activeWorkflow → Resume 或意图路由）
- openspec/changes/refund-workflow-demo/specs/refund-workflow/spec.md（入口点和意图路由需求，第 75-112 行）
- MAF 参考：ConditionalEdges/02_SwitchCase/Program.cs（使用 DashScope 端点的 OpenAI 客户端模式）
- MAF 参考：InProcessExecution.cs（RunStreamingAsync, RunAsync）
- src/CallCenter.Framework/Session/InMemorySessionStore.cs（Phase 1 完成后）
- src/CallCenter.Framework/Parsing/StructuredOutputParser.cs（Phase 1 完成后）
- src/CallCenter.Workflows/Refund/RefundMessages.cs（Phase 1 完成后，RefundIntent record）
</read_first>

<acceptance_criteria>
- src/CallCenter.AgentHost/EntryPoint.cs 存在
- 命名空间：CallCenter.AgentHost
- record IntentResult(string Intent, string? Workflow, string? OrderId)
- Class EntryPoint，包含：
  - 构造函数：EntryPoint(IChatClient chatClient, InMemorySessionStore sessionStore)
  - 私有字段：_intentAgent（AIAgent），_sessionStore（InMemorySessionStore）
- 方法：Task<IntentResult?> RecognizeIntentAsync(string userMessage, CancellationToken ct = default)
  - 创建系统提示："你是一个意图识别助手。分析用户消息，判断意图。返回JSON格式: {\"intent\": \"refund\"|\"greeting\"|\"unknown\", \"workflow\": \"RefundWorkflow\", \"orderId\": \"<如果提到订单号>\"}. 只返回JSON，不要其他内容。"
  - 调用 chatClient 发送消息给 AI Agent
  - 使用 StructuredOutputParser<IntentResult> 解析响应
  - 失败时返回 IntentResult 或 null
- 方法：Task<string?> GetActiveWorkflowAsync(string sessionId, CancellationToken ct = default)
  - 调用 sessionStore.GetAsync<string>("activeWorkflow", sessionId)
- 方法：Task SetActiveWorkflowAsync(string sessionId, string workflowName, CancellationToken ct = default)
  - 调用 sessionStore.SetAsync("activeWorkflow", workflowName, sessionId)
- 方法：Task ClearActiveWorkflowAsync(string sessionId, CancellationToken ct = default)
  - 调用 sessionStore.RemoveAsync("activeWorkflow", sessionId)
- 方法：Task<ProcessResult> ProcessAsync(string sessionId, string userMessage, Workflow refundWorkflow, CancellationToken ct = default)
  - 步骤1：从会话获取 activeWorkflow
  - 步骤2：如果 activeWorkflow 存在且 == "RefundWorkflow"：返回 ProcessResult.ResumeExisting（Phase 3 实现完整 Resume）
  - 步骤3：如果 activeWorkflow 存在但 != "RefundWorkflow"：清除它，继续进行意图识别
  - 步骤4：调用 RecognizeIntentAsync(userMessage, ct)
  - 步骤5：如果 intent == null 或 intent.Intent == "unknown"/"greeting"：返回 ProcessResult.NoIntent(回复消息)
  - 步骤6：如果 intent.Intent == "refund"：SetActiveWorkflowAsync(sessionId, "RefundWorkflow")，返回 ProcessResult.StartWorkflow(new RefundIntent(intent.OrderId, "U100"))
- record ProcessResult，包含子类型：ResumeExistingResult, StartWorkflowResult(RefundIntent), NoIntentResult(string)
- 必要的 using 语句：Microsoft.Extensions.AI、System.ClientModel、OpenAI、CallCenter.Framework.Session、CallCenter.Framework.Parsing、CallCenter.Workflows.Refund、Microsoft.Agents.AI.Workflows
</acceptance_criteria>

<action>
创建 EntryPoint.cs，包含上述所有方法。关键实现细节：

1. RecognizeIntentAsync：
   - 使用 AIAgent（通过 ChatClientAgent 包装 chatClient）
   - 用 StructuredOutputParser<IntentResult>.Parse(response.Text) 解析响应
   - 优雅处理 null/空响应

2. ProcessAsync：
   - 这是 ConsoleDemo 的主入口点
   - 返回一个区分类型的结果，ConsoleDemo 可以做模式匹配
   - Phase 2 中，ResumeExisting 只返回消息"工作流已存在"（完整 Resume 在 Phase 3）
   - StartWorkflow 返回 RefundIntent 传给 InProcessExecution.RunAsync
   - NoIntent 返回问候/回退消息

3. 使用构造函数注入 IChatClient 和 InMemorySessionStore（D-07, D-65）

4. 所有异步方法接受 CancellationToken，默认为 CancellationToken.None
</action>

### 任务 1.2：验证 EntryPoint 编译

<acceptance_criteria>
- `dotnet build src/CallCenter.AgentHost/CallCenter.AgentHost.csproj` 成功，0 错误
- EntryPoint 正确编译
- 无未使用变量警告
</acceptance_criteria>

<action>
对 AgentHost 项目执行 dotnet build。修复任何编译错误。
</action>
