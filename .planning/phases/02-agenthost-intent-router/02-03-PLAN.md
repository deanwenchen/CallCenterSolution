---
wave: 1
depends_on: []
files_modified:
  - src/CallCenter.AgentHost/EntryPoint.cs
  - src/CallCenter.ConsoleDemo/Program.cs
requirements: [SK-02]
autonomous: true
---

# 计划 03：SK-02 Gap Closure — AgentSkillsProvider 注册 RefundSkill

## 目标

关闭 VERIFICATION.md 中的 SK-02 gap：创建 `AgentSkillsProvider` 并注册 `RefundSkill`，将其注入到 Intent Agent 的 `AIContextProviders`，使 LLM 能通过 Frontmatter description 自动发现技能。

## 任务

### 任务 3.1：修改 EntryPoint 接受 AgentSkillsProvider

<read_first>
- src/CallCenter.AgentHost/EntryPoint.cs（当前实现）
- .planning/phases/02-agenthost-intent-router/02-CONTEXT.md（D-63~D-64：Skill 注册决策）
- .planning/phases/02-agenthost-intent-router/02-VERIFICATION.md（SK-02 gap 详情）
- MAF 参考：AgentSkillsProvider.cs（构造函数：`new AgentSkillsProvider(params AgentSkill[] skills)`）
- MAF 参考：ChatClientAgentOptions.cs（`AIContextProviders` 属性）
</read_first>

<acceptance_criteria>
- EntryPoint 构造函数新增 `AgentSkillsProvider? skillsProvider = null` 参数
- 当 skillsProvider 非 null 时，`ChatClientAgentOptions.AIContextProviders` 设置为 `[skillsProvider]`
- 现有两参数构造函数保持向后兼容（可选参数默认 null）
- `dotnet build src/CallCenter.AgentHost/CallCenter.AgentHost.csproj` 成功，0 错误
</acceptance_criteria>

<action>
修改 EntryPoint.cs 的构造函数：

1. 新增可选参数：`AgentSkillsProvider? skillsProvider = null`
2. 在创建 `_intentAgent` 时，将 `ChatClientAgentOptions` 改为：
```
new ChatClientAgentOptions
{
    ChatOptions = new() { Instructions = systemPrompt },
    AIContextProviders = skillsProvider != null ? [skillsProvider] : null,
}
```
3. 保持原有的 `EntryPoint(IChatClient chatClient, InMemorySessionStore sessionStore)` 签名向后兼容，通过可选参数实现
</action>

### 任务 3.2：Program.cs 创建并传入 AgentSkillsProvider

<read_first>
- src/CallCenter.ConsoleDemo/Program.cs（当前 DI 配置）
- src/CallCenter.AgentHost/Skills/RefundSkill.cs（已存在的 Skill 类）
- MAF 参考：AgentSkillsProvider.cs（构造函数接受 params AgentSkill[]）
- MAF 参考：AgentSkillsProviderBuilder.cs（UseSkill/UseOptions 链式 API）
</read_first>

<acceptance_criteria>
- Program.cs 在创建 EntryPoint 之前构建 AgentSkillsProvider
- 使用 `new AgentSkillsProvider(new RefundSkill())` 或等效的 Builder 模式
- AgentSkillsProvider 传入 EntryPoint 构造函数
- `dotnet build src/CallCenter.ConsoleDemo/CallCenter.ConsoleDemo.csproj` 成功，0 错误
- 添加必要的 using：`Microsoft.Agents.AI`
</acceptance_criteria>

<action>
在 Program.cs 中，在创建 EntryPoint 之前（约第 42 行）添加：

```csharp
// Create SkillsProvider with RefundSkill registered
var skillsProvider = new AgentSkillsProvider(new RefundSkill());
```

然后将第 43 行的 EntryPoint 创建改为：
```csharp
var entryPoint = new EntryPoint(chatClient, sessionStore, skillsProvider);
```

添加 using（文件顶部）：
```csharp
using CallCenter.AgentHost.Skills;
```

`AgentSkillsProvider` 的 `using Microsoft.Agents.AI;` 已在 EntryPoint.cs 中存在，Program.cs 需要添加相同的 using。
</action>

### 任务 3.3：验证编译

<acceptance_criteria>
- `dotnet build src/CallCenter.AgentHost/CallCenter.AgentHost.csproj` 成功，0 错误，0 新警告
- `dotnet build src/CallCenter.ConsoleDemo/CallCenter.ConsoleDemo.csproj` 成功，0 错误，0 新警告
- RefundSkill 通过 AgentSkillsProvider 注册后可被 LLM 发现
</acceptance_criteria>

<action>
对两个项目执行 dotnet build。验证无新增编译错误或警告。
</action>
