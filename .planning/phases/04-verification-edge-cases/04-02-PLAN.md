---
phase: 04-verification-edge-cases
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/CallCenter.ConsoleDemo/Program.cs
autonomous: false
requirements:
  - FW-01
  - FW-03
  - FW-04

must_haves:
  truths:
    - "Framework 14 个组件文件全部存在（5 个可用 + 9 个空壳）"
    - "所有空壳/TODO 文件包含明确的 TODO 注释说明实现意图"
    - "dotnet build 产生 0 warnings（不仅是 0 errors）"
    - "5 个 Phase 3 成功场景可手动验证通过"
    - "取消场景可验证：确认时输入'取消' → 流程终止 → 输出取消通知"
  artifacts:
    - path: src/CallCenter.Framework/EventBus/IBusinessEventBus.cs
      provides: "EventBus interface"
    - path: src/CallCenter.Framework/EventBus/InMemoryBusinessEventBus.cs
      provides: "In-memory event bus implementation (FW-01)"
    - path: src/CallCenter.Framework/EventBus/RefundEvents.cs
      provides: "Refund event types"
    - path: src/CallCenter.Framework/Parsing/StructuredOutputParser.cs
      provides: "JSON parsing with schema injection (FW-03)"
    - path: src/CallCenter.Framework/Safety/PiiRedactor.cs
      provides: "PII redaction (FW-04)"
    - path: src/CallCenter.Framework/Safety/KeywordFilter.cs
      provides: "Keyword filter stub"
    - path: src/CallCenter.Framework/Safety/PromptInjectionDetector.cs
      provides: "Prompt injection detector stub"
    - path: src/CallCenter.Framework/Safety/SafetyPipelineAgent.cs
      provides: "Safety pipeline agent stub"
    - path: src/CallCenter.Framework/Compaction/CompactionExtensions.cs
      provides: "Compaction stub"
    - path: src/CallCenter.Framework/Audit/AuditLogger.cs
      provides: "Audit logger stub"
    - path: src/CallCenter.Framework/Audit/AuditTrailMiddleware.cs
      provides: "Audit trail stub"
    - path: src/CallCenter.Framework/Saga/SagaBuilder.cs
      provides: "Saga builder stub"
    - path: src/CallCenter.Framework/Saga/SagaExtensions.cs
      provides: "Saga extensions stub"
    - path: src/CallCenter.Framework/Pipeline/StandardPipelineFactory.cs
      provides: "Standard pipeline factory stub"
    - path: src/CallCenter.Framework/ServiceCollectionExtensions.cs
      provides: "Service collection extensions"
  key_links:
    - from: dotnet build
      to: all 5 projects
      via: "slNX solution file"
      pattern: "dotnet build.*0 Warning"
---

<objective>
验证 Framework 14 个组件文件存在且有 TODO 注释（5 个可用 + 9 个空壳），手动验收 5 个 Phase 3 成功场景 + 取消场景，确认 dotnet build 0 warnings。

Purpose: 完成 Phase 4 的验收目标 — FW-01/FW-03/FW-04 已在 Phase 1 实现，需要验证文件存在和结构正确。取消场景需要在 04-01 修复后才能验证。
Output: 验证报告 + build 结果
</objective>

<execution_context>
@D:/Claude/CallCenterSolution1/.claude/get-shit-done/workflows/execute-plan.md
@D:/Claude/CallCenterSolution1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-verification-edge-cases/04-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-consoledemo-main-loop-event-handling/03-SUMMARY.md

# Current Program.cs (main loop, HandleRequest with cancel handling)
@src/CallCenter.ConsoleDemo/Program.cs

# Framework files to verify existence and TODO comments
@src/CallCenter.Framework/EventBus/InMemoryBusinessEventBus.cs
@src/CallCenter.Framework/Parsing/StructuredOutputParser.cs
@src/CallCenter.Framework/Safety/PiiRedactor.cs
@src/CallCenter.Framework/Safety/KeywordFilter.cs
@src/CallCenter.Framework/Safety/PromptInjectionDetector.cs
@src/CallCenter.Framework/Compaction/CompactionExtensions.cs
@src/CallCenter.Framework/Audit/AuditLogger.cs
@src/CallCenter.Framework/Saga/SagaBuilder.cs
@src/CallCenter.Framework/Pipeline/StandardPipelineFactory.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 验证 Framework 组件文件存在和 TODO 注释完整性</name>
  <files>（只读验证，不修改文件）</files>
  <read_first>
    - src/CallCenter.Framework/EventBus/InMemoryBusinessEventBus.cs
    - src/CallCenter.Framework/Parsing/StructuredOutputParser.cs
    - src/CallCenter.Framework/Safety/PiiRedactor.cs
    - src/CallCenter.Framework/Safety/KeywordFilter.cs
    - src/CallCenter.Framework/Safety/PromptInjectionDetector.cs
    - src/CallCenter.Framework/Compaction/CompactionExtensions.cs
    - src/CallCenter.Framework/Audit/AuditLogger.cs
    - src/CallCenter.Framework/Saga/SagaBuilder.cs
    - src/CallCenter.Framework/Pipeline/StandardPipelineFactory.cs
  </read_first>
  <action>
验证以下 14 个 Framework 组件文件（FW-01/FW-03/FW-04 相关）：

1. EventBus 组（FW-01，可用）：
   - IBusinessEventBus.cs — 接口定义
   - InMemoryBusinessEventBus.cs — 实现
   - RefundEvents.cs — 事件类型

2. Parsing 组（FW-03，可用）：
   - StructuredOutputParser.cs — JSON 解析

3. Safety 组（FW-04，PiiRedactor 可用，其余空壳）：
   - PiiRedactor.cs — PII 脱敏

4. 空壳/TODO 文件（9 个，确认存在且包含 TODO 注释）：
   - KeywordFilter.cs
   - PromptInjectionDetector.cs
   - SafetyPipelineAgent.cs
   - CompactionExtensions.cs
   - AuditLogger.cs
   - AuditTrailMiddleware.cs
   - SagaBuilder.cs
   - SagaExtensions.cs
   - StandardPipelineFactory.cs

对每个文件：
- 确认文件存在
- 确认包含 TODO 注释说明实现意图
- 如果是已实现的文件（EventBus, StructuredOutputParser, PiiRedactor），确认逻辑完整

创建验证清单，记录每个文件的验证结果。
  </action>
  <acceptance_criteria>
    - 9 个 Framework 组件文件全部存在
    - 已实现的文件（EventBus, StructuredOutputParser, PiiRedactor）逻辑完整
    - 空壳文件包含 TODO 注释说明实现意图
    - 无 orphan imports 或 broken references
  </acceptance_criteria>
  <verify>
    <automated>for f in EventBus/InMemoryBusinessEventBus.cs Parsing/StructuredOutputParser.cs Safety/PiiRedactor.cs Compaction/CompactionExtensions.cs Audit/AuditLogger.cs Saga/SagaBuilder.cs Pipeline/StandardPipelineFactory.cs; do test -f "src/CallCenter.Framework/$f" && echo "OK: $f" || echo "MISSING: $f"; done</automated>
    <manual>grep -rl "// TODO" src/CallCenter.Framework/ | wc -l — 确认 TODO 注释存在于空壳文件</manual>
  </verify>
  <done>所有 9 个 Framework 组件文件验证通过，TODO 注释完整</done>
</task>

<task type="auto">
  <name>Task 2: 验证 dotnet build 0 warnings</name>
  <files>（不修改文件，运行编译验证）</files>
  <read_first>
    - src/CallCenter.Solution.slnx
  </read_first>
  <action>
运行 `dotnet build --no-incremental` 验证全项目编译：
1. 确保 0 errors
2. 确保 0 warnings（Phase 4 目标）
3. 如有剩余 warnings，记录详细 warning 代码和来源

预期结果：0 errors, 0 warnings（04-01 移除了 System.Text.Json 引用，修复了 ExecuteRefundExecutor）
  </action>
  <acceptance_criteria>
    - dotnet build 输出 0 errors
    - dotnet build 输出 0 warnings
    - 所有 5 个项目编译通过
  </acceptance_criteria>
  <verify>
    <automated>dotnet build --no-incremental 2>&1 | tail -5</automated>
  </verify>
  <done>dotnet build 0 errors, 0 warnings</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 手动验收测试 — 6 个场景验证</name>
  <files>（手动测试，不修改文件）</files>
  <read_first>
    - src/CallCenter.ConsoleDemo/Program.cs
    - src/CallCenter.Shared/Services/MockOrderService.cs
  </read_first>
  <what-built>
Phase 1-4 完整实现：
- RefundWorkflow 完整执行（GetOrder → CheckRule → WaitConfirm → ExecuteRefund → RestoreCoupon → Notify）
- Intent Router 识别 refund/greeting/unknown 意图
- ConsoleDemo 主循环集成所有组件
- 取消路径已修复（ExecuteRefundExecutor 处理 Confirmed=false）
- 4 个 NU1510 警告已消除

Mock 订单数据（MockOrderService）：
- A001: 可退（7天内, 已签收, 普通品类）
- A002: 超期（>7天, 不可退）
- A003: 未签收（不可退）
  </what-built>
  <how-to-verify>
运行 `cd src/CallCenter.ConsoleDemo && dotnet run`（确保 DASHSCOPE_API_KEY 已设置），依次测试：

**场景 1 — 完整退款流程（A001 可退）：**
1. 输入: `我要退款，订单A001`
2. 预期: 查订单 → 校验通过 → 显示订单详情 → 询问确认
3. 输入: `确认`
4. 预期: 退款成功 → 输出退款结果（RefundId, Status）

**场景 2 — 动态追问（缺少订单号）：**
1. 输入: `我要退款`
2. 预期: 提示"请提供订单号"
3. 输入: `A001`
4. 预期: 继续流程，与场景 1 相同

**场景 3 — 规则校验失败（A002 超期）：**
1. 输入: `我要退款，订单A002`
2. 预期: 校验失败，输出"超过7天不可退"或类似提示

**场景 4 — 闲聊（无意图）：**
1. 输入: `你好`
2. 预期: 自然回复，不启动退款流程

**场景 5 — 取消流程：**
1. 输入: `我要退款，订单A001`
2. 预期: 查订单 → 校验通过 → 显示订单详情 → 询问确认
3. 输入: `取消`
4. 预期: 输出"退款已取消"，流程正常终止（不报错）

**场景 6 — 取消流程（通过 Program.cs HandleRequest）：**
1. 输入: `我要退款，订单A001`
2. 预期: 到达确认步骤
3. 输入: `取消`
4. 预期: 输出"退款已取消"，流程正常结束，不抛出异常

每个场景完成后输入 `quit` 退出，重新运行测试下一个场景。
  </how-to-verify>
  <resume-signal>
回复测试结果：每个场景是 PASS 还是 FAIL。如果 FAIL，描述实际输出和预期输出的差异。
</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 手动测试 → LLM API | 测试场景调用 DashScope API，API key 通过环境变量传入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | DASHSCOPE_API_KEY | mitigate | API key 通过环境变量传入，不硬编码 |
| T-04-04 | Tampering | Build verification output | accept | 编译输出只读，不修改构建配置 |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 无包安装，纯验证操作 |
</threat_model>

<verification>
- 9 个 Framework 组件文件验证通过
- dotnet build 0 errors, 0 warnings
- 6 个手动验收场景全部 PASS
</verification>

<success_criteria>
- 所有 Framework 组件文件存在，TODO 注释完整
- dotnet build 0 errors, 0 warnings
- 6 个手动验收场景（5 个 Phase 3 场景 + 取消场景）全部通过
- Phase 4 所有要求（WF-04, FW-01, FW-03, FW-04）验证完成
</success_criteria>

<output>
Create `.planning/phases/04-verification-edge-cases/04-02-SUMMARY.md` when done
</output>
