---
wave: 2
depends_on: [08-01]
files_modified:
  - src/CallCenter.Workflows/Exchange/ExchangeWorkflow.cs
  - src/CallCenter.Workflows/Exchange/ExchangeMessages.cs
  - src/CallCenter.Workflows/Exchange/Executors/*.cs
  - src/CallCenter.AgentHost/Skills/ExchangeSkill.cs
autonomous: true
---

# Phase 8: Business Extensibility Guide — Plan 02

## Objective

Create ExchangeSkill skeleton by following the 7-step guide — proof that the guide works. All handlers throw NotImplementedException. Must compile.

## Requirements Addressed

BE-01

## Must Haves

- ExchangeWorkflow.cs with ports/edges matching Refund pattern
- ExchangeMessages.cs with skeleton records
- Executors with NotImplementedException handlers
- ExchangeSkill.cs with skeleton registration
- `dotnet build` passes (0 errors)

## Tasks

### Task 1: Create Exchange directory structure and messages

<read_first>
- src/CallCenter.Workflows/Refund/RefundMessages.cs (template for ExchangeMessages)
- src/CallCenter.Workflows/Refund/RefundWorkflow.cs (template for ExchangeWorkflow)
</read_first>

<action>
Create `src/CallCenter.Workflows/Exchange/ExchangeMessages.cs`:
- Copy structure from RefundMessages.cs
- Replace all `Refund` → `Exchange`:
  - `RefundIntent` → `ExchangeIntent`
  - `RefundSignal` → `ExchangeSignal` (enum: Init, NeedOrderId, OrderFound, Ineligible, Cancelled)
  - `OrderFound` → `ExchangeOrderFound`
  - `RefundRuleResult` → `ExchangeRuleResult`
  - `ConfirmRefundRequest` → `ConfirmExchangeRequest`
  - `UserConfirmation` → `UserConfirmation` (shared, no rename)
  - `RefundExecuted` → `ExchangeExecuted`
  - `CouponRestored` → `CouponRestored` (shared)
  - `RefundNotification` → `ExchangeNotification`

Create `src/CallCenter.Workflows/Exchange/Executors/` directory (empty for now — populated in Task 2).
</action>

<acceptance_criteria>
- ExchangeMessages.cs compiles with correct namespace `CallCenter.Workflows.Exchange`
- All message records defined (matching Refund pattern)
- No `Refund` references remain
</acceptance_criteria>

### Task 2: Create skeleton executors

<read_first>
- src/CallCenter.Workflows/Refund/Executors/GetOrderExecutor.cs (template)
- src/CallCenter.Workflows/Refund/Executors/CheckRefundRuleExecutor.cs (template)
- src/CallCenter.Workflows/Refund/Executors/WaitUserConfirmExecutor.cs (template)
- src/CallCenter.Workflows/Refund/Executors/ExecuteRefundExecutor.cs (template)
- src/CallCenter.Workflows/Refund/Executors/RestoreCouponExecutor.cs (template)
- src/CallCenter.Workflows/Refund/Executors/SendNotificationExecutor.cs (template)
- src/CallCenter.Workflows/Refund/Executors/RefundDeniedExecutor.cs (template)
</read_first>

<action>
Create executor skeleton files in `src/CallCenter.Workflows/Exchange/Executors/`:

For each Refund executor, create Exchange equivalent:
1. `GetExchangeOrderExecutor.cs` — copy GetOrderExecutor, replace Refund → Exchange, Handler body: `throw new NotImplementedException("Exchange workflow not implemented — this is a skeleton created via the Business Extensibility Guide")`
2. `CheckExchangeRuleExecutor.cs` — same pattern
3. `WaitExchangeConfirmExecutor.cs` — same pattern
4. `ExecuteExchangeExecutor.cs` — same pattern
5. `ExchangeRestoreCouponExecutor.cs` — same pattern (or reuse RestoreCouponExecutor if shared)
6. `ExchangeSendNotificationExecutor.cs` — same pattern
7. `ExchangeDeniedExecutor.cs` — same pattern

Each executor must:
- Keep the same `[SendsMessage]` attributes (with Exchange message types)
- Keep the same constructor pattern (service injection + parameterless fallback)
- Keep the same `HandleAsync` signature
- Body: single `throw NotImplementedException(...)` with the standard message

Exception: `RestoreCouponExecutor` can be shared between Refund and Exchange (same logic), so either copy it or add a note in the guide about shared executors.
</action>

<acceptance_criteria>
- All 7 executor files created in Exchange/Executors/
- Each executor throws NotImplementedException with standard message
- Namespaces: `CallCenter.Workflows.Exchange.Executors`
- [SendsMessage] attributes reference Exchange message types
</acceptance_criteria>

### Task 3: Create ExchangeWorkflow.cs

<read_first>
- src/CallCenter.Workflows/Refund/RefundWorkflow.cs (exact template)
</read_first>

<action>
Create `src/CallCenter.Workflows/Exchange/ExchangeWorkflow.cs`:

Copy structure from RefundWorkflow.cs:
- Class: `public static class ExchangeWorkflow`
- Method: `public static Workflow Build(...)` with same service parameters
- Ports: `infoPort = RequestPort.Create<ExchangeSignal, ExchangeIntent>("ExchangeInfoPort")`
- Confirm port: `confirmPort = RequestPort.Create<ConfirmExchangeRequest, UserConfirmation>("ExchangeConfirmPort")`
- Executors: instantiate all 7 Exchange executors
- Edges: same topology as Refund (ForwardMessage, AddEdge with conditions)
- Output: `builder.WithOutputFrom(notify, denied).Build()`

Replace all `Refund` → `Exchange` references.
</action>

<acceptance_criteria>
- ExchangeWorkflow.cs compiles (references ExchangeMessages and Exchange executors)
- Workflow has same topology as RefundWorkflow
- Namespace: `CallCenter.Workflows.Exchange`
</acceptance_criteria>

### Task 4: Create ExchangeSkill.cs

<read_first>
- src/CallCenter.AgentHost/Skills/RefundSkill.cs (exact template)
</read_first>

<action>
Create `src/CallCenter.AgentHost/Skills/ExchangeSkill.cs`:

Copy structure from RefundSkill.cs:
- Class: `public sealed class ExchangeSkill : AgentClassSkill<ExchangeSkill>`
- Frontmatter: name "exchange", description "处理用户换货请求。当用户要求换货时使用。"
- Instructions: skeleton instructions for exchange flow
- Skill scripts: empty or placeholder (no real MCP calls needed for skeleton)

Register ExchangeSkill in Program.cs (or note in guide):
- Add `new ExchangeSkill()` to the `AgentSkillsProvider` constructor alongside `RefundSkill`
</action>

<acceptance_criteria>
- ExchangeSkill.cs compiles with valid AgentClassSkill structure
- Frontmatter has name "exchange" and Chinese description
- Program.cs references ExchangeSkill (or has commented registration)
</acceptance_criteria>

### Task 5: Build verification

<action>
Run `dotnet build` and verify 0 errors. If there are errors:
- Check namespace consistency across all Exchange files
- Check message type references in ExchangeWorkflow.cs
- Check [SendsMessage] attributes match actual record names
- Check using directives in all files
</action>

<acceptance_criteria>
- `dotnet build`: 0 errors, 0 warnings
- Exchange directory structure exists and matches guide's expected layout
- All Exchange files use `CallCenter.Workflows.Exchange` namespace
</acceptance_criteria>

---

*Phase 8 Plan 02 — Exchange Skeleton (guide verification)*
