---
phase: 09-framework-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/CallCenter.Framework/CallCenterOptions.cs
  - src/CallCenter.AgentHost/Extensions.cs
autonomous: true
requirements:
  - DI-01
  - DI-03
  - DI-04
must_haves:
  truths:
    - "CallCenterOptions.ApplyDefaults() is the ONLY place that reads Environment.GetEnvironmentVariable(\"DASHSCOPE_API_KEY\")"
    - "Extensions.cs provides AddCallCenter(options?) that registers all base infrastructure"
    - "Mock services (MockOrderService, MockFinanceService, MockMemberService) are registered by default"
    - "Service override methods exist: AddCallCenterOrderService<T>(), AddCallCenterFinanceService<T>(), AddCallCenterMemberService<T>()"
  artifacts:
    - path: "src/CallCenter.Framework/CallCenterOptions.cs"
      provides: "Configuration class with ApiKey, ModelName, Endpoint, UseMockServices, OnRefundCompleted, ApplyDefaults()"
      contains: "public void ApplyDefaults()"
    - path: "src/CallCenter.AgentHost/Extensions.cs"
      provides: "DI extension methods for IServiceCollection"
      exports: ["AddCallCenter", "AddCallCenterOrderService", "AddCallCenterFinanceService", "AddCallCenterMemberService"]
  key_links:
    - from: "src/CallCenter.Framework/CallCenterOptions.cs"
      to: "src/CallCenter.AgentHost/Extensions.cs"
      via: "Extensions uses CallCenterOptions to configure API key and mock service toggle"
      pattern: "options.*ApplyDefaults|options.*ApiKey"
    - from: "src/CallCenter.AgentHost/Extensions.cs"
      to: "src/CallCenter.Framework/Pipeline/StandardPipelineFactory.cs"
      via: "Extensions calls StandardPipelineFactory.CreatePipeline() and CreateSummarizerClient()"
      pattern: "StandardPipelineFactory\\.Create"
---

<objective>
Create the configuration layer (CallCenterOptions) and DI extension (Extensions.cs) that form the foundation for the CallCenter framework extraction.

Purpose: Centralize environment variable reading in one place (ApplyDefaults) and provide a single DI registration method (AddCallCenter) that replaces the 80+ lines of infrastructure setup currently in Program.cs.

Output: Two new files — CallCenterOptions.cs and Extensions.cs
</objective>

<execution_context>
@D:/Claude/CallCenterSolution1/.claude/get-shit-done/workflows/execute-plan.md
@D:/Claude/CallCenterSolution1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-framework-extraction/09-CONTEXT.md
@D:/Claude/CallCenterSolution1/openspec/changes/extract-callcenter-service/specs/di-support/spec.md

# Reference: existing Program.cs infrastructure setup (lines 25-91)
@D:/Claude/CallCenterSolution1/src/CallCenter.ConsoleDemo/Program.cs

# Reference: StandardPipelineFactory patterns to replicate
@D:/Claude/CallCenterSolution1/src/CallCenter.Framework/Pipeline/StandardPipelineFactory.cs

# Reference: old DI extension (3 lines, to be replaced)
@D:/Claude/CallCenterSolution1/src/CallCenter.Framework/ServiceCollectionExtensions.cs

# Reference: Mock service implementations and MCP interfaces
@D:/Claude/CallCenterSolution1/src/CallCenter.Shared/Services/MockOrderService.cs
@D:/Claude/CallCenterSolution1/src/CallCenter.Shared/Services/MockFinanceService.cs
@D:/Claude/CallCenterSolution1/src/CallCenter.Shared/Services/MockMemberService.cs
@D:/Claude/CallCenterSolution1/src/CallCenter.Shared/Mcp/IOrderMcpClient.cs
@D:/Claude/CallCenterSolution1/src/CallCenter.Shared/Mcp/IFinanceMcpClient.cs
@D:/Claude/CallCenterSolution1/src/CallCenter.Shared/Mcp/IMemberMcpClient.cs

# Reference: RefundCompletedEvent for OnRefundCompleted callback
@D:/Claude/CallCenterSolution1/src/CallCenter.Framework/EventBus/RefundEvents.cs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create CallCenterOptions configuration class</name>
  <files>src/CallCenter.Framework/CallCenterOptions.cs</files>
  <read_first>
    src/CallCenter.ConsoleDemo/Program.cs (lines 25-36: apiKey, modelName, endpoint extraction)
    src/CallCenter.Framework/ServiceCollectionExtensions.cs (existing pattern for Framework namespace)
  </read_first>
  <behavior>
    - Test 1: new CallCenterOptions().ApplyDefaults() with DASHSCOPE_API_KEY env set → ApiKey populated
    - Test 2: new CallCenterOptions { ApiKey = "sk-override" }.ApplyDefaults() → ApiKey remains "sk-override" (env not read)
    - Test 3: ApplyDefaults() with no env and no explicit key → ApiKey is null/empty
    - Test 4: Default ModelName = "qwen3-vl-flash" (matches Program.cs line 28), Endpoint = "https://dashscope.aliyuncs.com/compatible-mode/v1" (matches Program.cs line 34)
    - Test 5: UseMockServices defaults to true (per D-03)
    - Test 6: OnRefundCompleted is Action<RefundCompletedEvent>? (nullable callback)
  </behavior>
  <action>
    Create src/CallCenter.Framework/CallCenterOptions.cs in namespace CallCenter.Framework.

    Define public class CallCenterOptions with these properties:
    - string? ApiKey { get; set; } — null by default
    - string ModelName { get; set; } = "qwen3-vl-flash" (matches Program.cs line 28 default)
    - string Endpoint { get; set; } = "https://dashscope.aliyuncs.com/compatible-mode/v1" (matches Program.cs line 34)
    - bool UseMockServices { get; set; } = true (per D-03: default to mock services)
    - Action<CallCenter.Framework.EventBus.RefundCompletedEvent>? OnRefundCompleted { get; set; } — nullable callback for refund completion event (matches Program.cs lines 49-53 event subscription pattern; type defined in src/CallCenter.Framework/EventBus/RefundEvents.cs)

    Define public void ApplyDefaults() method:
    - If ApiKey is null or empty, set it to Environment.GetEnvironmentVariable("DASHSCOPE_API_KEY")
    - If ModelName is still the default, check Environment.GetEnvironmentVariable("DASHSCOPE_MODEL_NAME") and override if set (matches Program.cs line 28 fallback pattern)
    - This is the ONLY place in the entire codebase that reads DASHSCOPE_API_KEY environment variable (per DI-03)

    Use namespace CallCenter.Framework. No other classes in this file.
  </action>
  <verify>
    <automated>dotnet build src/CallCenter.Framework/CallCenter.Framework.csproj</automated>
    <source>src/CallCenter.Framework/CallCenterOptions.cs contains "ApplyDefaults()" and "Environment.GetEnvironmentVariable(\"DASHSCOPE_API_KEY\")"</source>
    <source>grep -rn "DASHSCOPE_API_KEY" src/ --include="*.cs" | grep -v "CallCenterOptions.cs" has zero results (only CallCenterOptions.cs reads this env var)</source>
  </verify>
  <done>CallCenterOptions.cs compiles, has all 5 properties + ApplyDefaults(), ApplyDefaults is the sole reader of DASHSCOPE_API_KEY env var</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Extensions.cs DI registration methods</name>
  <files>src/CallCenter.AgentHost/Extensions.cs</files>
  <read_first>
    src/CallCenter.ConsoleDemo/Program.cs (lines 25-91: full infrastructure setup to replicate in DI)
    src/CallCenter.Framework/Pipeline/StandardPipelineFactory.cs (CreatePipeline, CreateSummarizerClient signatures)
    src/CallCenter.Framework/ServiceCollectionExtensions.cs (existing AddCallCenter pattern — will coexist)
    openspec/changes/extract-callcenter-service/specs/di-support/spec.md (DI requirements)
  </read_first>
  <behavior>
    - Test 1: services.AddCallCenter() registers IOrderMcpClient → MockOrderService (per D-03)
    - Test 2: services.AddCallCenter().AddCallCenterOrderService<RealOrderService>() → IOrderMcpClient resolves to RealOrderService (per DI-04)
    - Test 3: IChatClient resolved from DI is the pipeline-wrapped client (not raw base)
    - Test 4: IChatClient keyed "base" resolves to raw OpenAI client
  </behavior>
  <action>
    Create src/CallCenter.AgentHost/Extensions.cs in namespace CallCenter.AgentHost.

    This file provides the NEW AddCallCenter() extension that replaces the 3-line version in ServiceCollectionExtensions.cs. The old version (Framework namespace) stays untouched for Phase 9 (Phase 12 removes it).

    Extension method 1: public static IServiceCollection AddCallCenter(this IServiceCollection services, CallCenterOptions? options = null)
    
    Implementation (follow Program.cs lines 25-91 as the blueprint):
    1. options ??= new CallCenterOptions(); options.ApplyDefaults();
    2. Validate options.ApiKey is not null/empty — throw InvalidOperationException with same message as Program.cs line 26
    3. Create OpenAIClient with apiKey + endpoint (per Program.cs lines 32-36)
    4. Register raw IChatClient as keyed singleton: services.AddKeyedSingleton&lt;IChatClient&gt;("base", baseClient) — per DI-02
    5. Create summarizerClient via StandardPipelineFactory.CreateSummarizerClient(openAIClient, options.ModelName)
    6. Create pipelineClient via StandardPipelineFactory.CreatePipeline(baseClient, summarizerClient, sessionId: "pipeline", logger: null) — sessionId "pipeline" per D-01
    7. Register pipelineClient as default singleton: services.AddSingleton&lt;IChatClient&gt;(pipelineClient) — per DI-02
    8. Register SessionStore: services.AddSingleton&lt;InMemorySessionStore&gt;()
    9. Register AuditLogger: services.AddSingleton(new AuditLogger(".audit"))
    10. Register EventBus: services.AddSingleton&lt;IBusinessEventBus, InMemoryBusinessEventBus&gt;()
    11. Register Mock services (per D-03): services.AddSingleton&lt;IOrderMcpClient, MockOrderService&gt;(), services.AddSingleton&lt;IFinanceMcpClient, MockFinanceService&gt;(), services.AddSingleton&lt;IMemberMcpClient, MockMemberService&gt;()
    12. Register Workflow: services.AddSingleton&lt;Workflow&gt;(sp => RefundWorkflow.Build(sp.GetRequiredService&lt;IOrderMcpClient&gt;(), sp.GetRequiredService&lt;IFinanceMcpClient&gt;(), sp.GetRequiredService&lt;IMemberMcpClient&gt;(), sp.GetRequiredService&lt;IBusinessEventBus&gt;()))
    13. Subscribe OnRefundCompleted callback if provided (match Program.cs lines 49-53 pattern)
    14. Return services

    Extension method 2: public static IServiceCollection AddCallCenterOrderService&lt;T&gt;(this IServiceCollection services) where T : class, IOrderMcpClient
    - services.AddSingleton&lt;IOrderMcpClient, T&gt;(); return services;

    Extension method 3: public static IServiceCollection AddCallCenterFinanceService&lt;T&gt;(this IServiceCollection services) where T : class, IFinanceMcpClient
    - services.AddSingleton&lt;IFinanceMcpClient, T&gt;(); return services;

    Extension method 4: public static IServiceCollection AddCallCenterMemberService&lt;T&gt;(this IServiceCollection services) where T : class, IMemberMcpClient
    - services.AddSingleton&lt;IMemberMcpClient, T&gt;(); return services;

    Required using statements: System, Microsoft.Extensions.DependencyInjection, Microsoft.Extensions.AI, OpenAI, System.ClientModel, CallCenter.Framework, CallCenter.Framework.Audit, CallCenter.Framework.EventBus, CallCenter.Framework.Session, CallCenter.Framework.Pipeline, CallCenter.Shared.Mcp, CallCenter.Shared.Services, CallCenter.Workflows.Refund.

    NOTE: Do NOT register AIAgentFactory, EntryPoint, or Workflow in this file — those are covered by AF-03 in Plan 02 (AIAgentFactory needs to exist first). For Phase 9, this file provides the infrastructure foundation only.
  </action>
  <verify>
    <automated>dotnet build src/CallCenter.AgentHost/CallCenter.AgentHost.csproj</automated>
    <source>src/CallCenter.AgentHost/Extensions.cs contains "AddCallCenter" and "AddCallCenterOrderService" and "AddCallCenterFinanceService" and "AddCallCenterMemberService"</source>
    <source>src/CallCenter.AgentHost/Extensions.cs contains "AddKeyedSingleton" for "base" key</source>
  </verify>
  <done>Extensions.cs compiles, has AddCallCenter() with full infrastructure registration + 3 override methods, IChatClient keyed "base" registered, pipeline client registered as default</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment→Application | DASHSCOPE_API_KEY read from environment — if missing, fail fast with clear error |
| DI Container→Service Resolution | Service override methods allow caller to replace Mock implementations — must honor registration order (last wins) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | CallCenterOptions.ApplyDefaults() | mitigate | ApiKey never logged or exposed; only passed to OpenAIClient constructor |
| T-09-02 | Tampering | DI service registration | accept | Internal DI registration — no external tampering surface |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
1. dotnet build succeeds for both CallCenter.Framework and CallCenter.AgentHost projects
2. CallCenterOptions.cs is the only file reading DASHSCOPE_API_KEY env var (grep confirms)
3. Extensions.cs contains all 4 extension methods with correct signatures
</verification>

<success_criteria>
- CallCenterOptions.cs exists with 5 properties and ApplyDefaults() method
- ApplyDefaults() reads DASHSCOPE_API_KEY only when ApiKey not explicitly set
- Extensions.cs exists with AddCallCenter() and 3 override methods
- AddCallCenter() registers all infrastructure: IChatClient (keyed + pipeline), SessionStore, AuditLogger, EventBus, Mock services, Workflow
- Project compiles without errors or new warnings
</success_criteria>

<output>
Create `.planning/phases/09-framework-extraction/09-01-SUMMARY.md` when done
</output>
