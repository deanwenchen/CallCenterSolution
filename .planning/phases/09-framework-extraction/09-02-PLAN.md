---
phase: 09-framework-extraction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/CallCenter.AgentHost/AIAgentFactory.cs
autonomous: true
requirements:
  - AF-01
  - AF-02
  - AF-03
must_haves:
  truths:
    - "AIAgentFactory.CreateIntentAgent() returns an AIAgent configured with IntentRegistry.BuildSystemPrompt() as Instructions"
    - "AIAgentFactory.CreateDialogAgent() returns an AIAgent configured with workflow conversation System Prompt"
    - "AIAgentFactory constructor receives IChatClient pipelineClient from DI"
    - "Both created agents use the same underlying pipeline client (safety, logging, compaction layers preserved)"
  artifacts:
    - path: "src/CallCenter.AgentHost/AIAgentFactory.cs"
      provides: "Factory class with CreateIntentAgent and CreateDialogAgent methods"
      contains: "class AIAgentFactory"
      min_lines: 20
  key_links:
    - from: "src/CallCenter.AgentHost/AIAgentFactory.cs"
      to: "src/CallCenter.AgentHost/IntentRegistry.cs"
      via: "CreateIntentAgent calls IntentRegistry.BuildSystemPrompt()"
      pattern: "IntentRegistry\\.BuildSystemPrompt"
    - from: "src/CallCenter.AgentHost/AIAgentFactory.cs"
      to: "src/CallCenter.AgentHost/EntryPoint.cs"
      via: "EntryPoint constructor receives AIAgentFactory and calls factory.CreateIntentAgent()"
      pattern: "factory\\.CreateIntentAgent"
---

<objective>
Create AIAgentFactory that wraps IChatClient pipeline and produces AIAgent instances for different scenarios (intent recognition vs workflow dialog).

Purpose: Decouple AIAgent creation from EntryPoint (currently hardcoded in EntryPoint constructor lines 52-61). The factory allows different System Prompts and skill configurations per agent type while sharing the same IChatClient pipeline.

Output: One new file — AIAgentFactory.cs
</objective>

<execution_context>
@D:/Claude/CallCenterSolution1/.claude/get-shit-done/workflows/execute-plan.md
@D:/Claude/CallCenterSolution1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-framework-extraction/09-CONTEXT.md
@D:/Claude/CallCenterSolution1/openspec/changes/extract-callcenter-service/specs/agent-factory/spec.md

# Reference: Current AIAgent creation in EntryPoint constructor (the code being replaced)
@D:/Claude/CallCenterSolution1/src/CallCenter.AgentHost/EntryPoint.cs

# Reference: IntentRegistry.BuildSystemPrompt() output format
@D:/Claude/CallCenterSolution1/src/CallCenter.AgentHost/IntentRegistry.cs

# Reference: SkillRegistry and AgentSkillsProvider patterns
@D:/Claude/CallCenterSolution1/src/CallCenter.AgentHost/Skills/SkillRegistry.cs

# Reference: StandardPipelineFactory for pipeline client understanding
@D:/Claude/CallCenterSolution1/src/CallCenter.Framework/Pipeline/StandardPipelineFactory.cs

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/CallCenter.AgentHost/EntryPoint.cs (current pattern to replicate in factory):
```csharp
// Current AIAgent creation — this is what AIAgentFactory.CreateIntentAgent() replaces:
_intentAgent = new ChatClientAgent(
    chatClient,
    new ChatClientAgentOptions
    {
        ChatOptions = new()
        {
            Instructions = IntentRegistry.BuildSystemPrompt(),
        },
        AIContextProviders = skillsProvider != null ? [skillsProvider] : null,
    });
```

From src/CallCenter.AgentHost/IntentRegistry.cs:
```csharp
public static string BuildSystemPrompt(); // Returns system prompt for intent recognition
public static bool IsBusinessIntent(string intent); // Check if intent has parameters
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create AIAgentFactory with CreateIntentAgent and CreateDialogAgent</name>
  <files>src/CallCenter.AgentHost/AIAgentFactory.cs</files>
  <read_first>
    src/CallCenter.AgentHost/EntryPoint.cs (lines 48-61: current ChatClientAgent creation pattern to extract into factory)
    src/CallCenter.AgentHost/IntentRegistry.cs (BuildSystemPrompt return format)
    src/CallCenter.AgentHost/Skills/SkillRegistry.cs (AgentSkillsProvider usage)
    src/CallCenter.Framework/Pipeline/StandardPipelineFactory.cs (pipeline client structure)
    openspec/changes/extract-callcenter-service/specs/agent-factory/spec.md (factory requirements)
  </read_first>
  <behavior>
    - Test 1: factory.CreateIntentAgent(skillsProvider) returns AIAgent with IntentRegistry.BuildSystemPrompt() as Instructions
    - Test 2: factory.CreateIntentAgent(null) returns AIAgent with no AIContextProviders (skillsProvider is nullable)
    - Test 3: factory.CreateDialogAgent(skillsProvider) returns AIAgent with workflow dialog System Prompt as Instructions
    - Test 4: Both agents use the same IChatClient instance that was passed to the factory constructor
  </behavior>
  <action>
    Create src/CallCenter.AgentHost/AIAgentFactory.cs in namespace CallCenter.AgentHost.

    Define public class AIAgentFactory:
    - Private readonly field: IChatClient _pipelineClient
    - Constructor: public AIAgentFactory(IChatClient pipelineClient) — stores the pipeline-wrapped IChatClient from DI

    Define public AIAgent CreateIntentAgent(AgentSkillsProvider? skillsProvider = null):
    - Return new ChatClientAgent(_pipelineClient, new ChatClientAgentOptions { ChatOptions = new() { Instructions = IntentRegistry.BuildSystemPrompt() }, AIContextProviders = skillsProvider != null ? [skillsProvider] : null })
    - This is an EXACT extraction of EntryPoint constructor lines 52-61 into a factory method

    Define public AIAgent CreateDialogAgent(AgentSkillsProvider? skillsProvider = null):
    - Return new ChatClientAgent(_pipelineClient, new ChatClientAgentOptions { ChatOptions = new() { Instructions = GetDialogSystemPrompt() }, AIContextProviders = skillsProvider != null ? [skillsProvider] : null })
    - GetDialogSystemPrompt() returns a basic system prompt for workflow dialog: "你是一个专业的客服助手，帮助用户完成退款、换货等业务操作。请按照工作流程的指引，逐步协助用户完成操作。" (per CONTEXT.md: DialogAgent System Prompt content TBD — use placeholder for Phase 9)

    Required using statements: Microsoft.Agents.AI, Microsoft.Extensions.AI, System.

    NOTE: Do NOT add DI registration for AIAgentFactory in this file. It will be registered in Extensions.cs (Plan 01) or via manual registration. Per CONTEXT.md D-03, the DI extension registers infrastructure; AIAgentFactory registration depends on EntryPoint constructor change (AF-03), which is handled in Plan 03.
  </action>
  <verify>
    <automated>dotnet build src/CallCenter.AgentHost/CallCenter.AgentHost.csproj</automated>
    <source>src/CallCenter.AgentHost/AIAgentFactory.cs contains "class AIAgentFactory" and "CreateIntentAgent" and "CreateDialogAgent"</source>
    <source>src/CallCenter.AgentHost/AIAgentFactory.cs contains "IntentRegistry.BuildSystemPrompt()"</source>
  </verify>
  <done>AIAgentFactory.cs compiles, has constructor accepting IChatClient, CreateIntentAgent uses IntentRegistry.BuildSystemPrompt(), CreateDialogAgent uses dialog system prompt</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Factory→LLM Pipeline | Factory passes IChatClient through — no modification of pipeline behavior, no injection of malicious instructions |
| System Prompt→Agent | System Prompts are hardcoded strings — must not leak sensitive internal logic details |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | AIAgentFactory.CreateIntentAgent() | mitigate | System Prompt sourced only from IntentRegistry.BuildSystemPrompt() — no external input |
| T-09-04 | Repudiation | AIAgent creation | accept | Factory is stateless — no creation audit needed at this level (AuditLogger handles downstream) |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
1. dotnet build succeeds for CallCenter.AgentHost
2. AIAgentFactory.cs contains both CreateIntentAgent and CreateDialogAgent methods
3. CreateIntentAgent references IntentRegistry.BuildSystemPrompt()
4. Both methods accept nullable AgentSkillsProvider
</verification>

<success_criteria>
- AIAgentFactory.cs exists in CallCenter.AgentHost namespace
- Constructor accepts IChatClient pipelineClient
- CreateIntentAgent(skillsProvider?) creates ChatClientAgent with IntentRegistry.BuildSystemPrompt() as Instructions
- CreateDialogAgent(skillsProvider?) creates ChatClientAgent with workflow dialog System Prompt as Instructions
- Both methods are functionally equivalent to extracting EntryPoint constructor lines 52-61 into a factory
- Project compiles without errors
</success_criteria>

<output>
Create `.planning/phases/09-framework-extraction/09-02-SUMMARY.md` when done
</output>
