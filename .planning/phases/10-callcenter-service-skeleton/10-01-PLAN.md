---
phase: 10-callcenter-service-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/CallCenter.AgentHost/CallCenterService.Core.cs
autonomous: true
requirements: [CS-02]

must_haves:
  truths:
    - "CallCenterService partial class exists with correct namespace"
    - "无参构造函数能自建 DI 容器并 resolve 所有依赖"
    - "DI 注入构造函数接受外部 IServiceProvider，不自建容器"
    - "类实现 IDisposable 释放自建 ServiceProvider"
  artifacts:
    - path: "src/CallCenter.AgentHost/CallCenterService.Core.cs"
      provides: "类定义、字段、双构造函数、Dispose"
      min_lines: 60
      contains: "public partial class CallCenterService"
  key_links:
    - from: "CallCenterService.Core.cs"
      to: "Extensions.cs (AddCallCenter)"
      via: "services.AddCallCenter(options)"
      pattern: "AddCallCenter"
    - from: "CallCenterService.Core.cs"
      to: "AIAgentFactory.cs"
      via: "services.AddSingleton<AIAgentFactory>()"
      pattern: "AddSingleton<AIAgentFactory>"
---

<objective>
创建 CallCenterService.Core.cs — partial class 骨架、字段声明、双构造函数模式、IDisposable 实现

Purpose: 这是整个 CallCenterService 的基础，定义所有依赖字段和两种实例化方式，后续 Intent/Routing/Execution/Interaction 所有 partial 文件都依赖此文件存在
Output: src/CallCenter.AgentHost/CallCenterService.Core.cs
</objective>

<execution_context>
@D:/Claude/CallCenterSolution1/.claude/get-shit-done/workflows/execute-plan.md
@D:/Claude/CallCenterSolution1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-callcenter-service-skeleton/10-CONTEXT.md
@openspec/changes/extract-callcenter-service/specs/callcenter-service/spec.md
@openspec/changes/extract-callcenter-service/design.md

# Source of truth — existing DI registration pattern
@src/CallCenter.AgentHost/Extensions.cs
@src/CallCenter.AgentHost/AIAgentFactory.cs
@src/CallCenter.Framework/CallCenterOptions.cs
@src/CallCenter.Framework/Pipeline/StandardPipelineFactory.cs
@src/CallCenter.Framework/Saga/SagaBuilder.cs
@src/CallCenter.Framework/Audit/AuditLogger.cs
@src/CallCenter.Framework/Audit/AuditTrailMiddleware.cs
@src/CallCenter.Framework/Session/InMemorySessionStore.cs
@src/CallCenter.Framework/EventBus/InMemoryBusinessEventBus.cs
@src/CallCenter.Framework/Logging/JsonlLogger.cs
@src/CallCenter.AgentHost/IntentRegistry.cs
@src/CallCenter.AgentHost/EntryPoint.cs
@src/CallCenter.ConsoleDemo/Program.cs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 创建 CallCenterService.Core.cs — partial class 定义、字段、双构造函数、IDisposable</name>
  <files>src/CallCenter.AgentHost/CallCenterService.Core.cs</files>
  <read_first>
    - src/CallCenter.AgentHost/Extensions.cs — AddCallCenter() 注册模式，构造函数需要调用此方法
    - src/CallCenter.AgentHost/AIAgentFactory.cs — 需要在无参构造函数中注册
    - src/CallCenter.Framework/CallCenterOptions.cs — 构造函数参数类型
    - src/CallCenter.ConsoleDemo/Program.cs — 当前所有依赖声明（第 25-92 行），字段需要覆盖这些依赖
    - src/CallCenter.AgentHost/EntryPoint.cs — 对照其字段声明模式
  </read_first>
  <action>
在 src/CallCenter.AgentHost/CallCenterService.Core.cs 中创建 `public partial class CallCenterService : IDisposable`，namespace 为 `CallCenter.AgentHost`。

**字段声明**（参照 Program.cs 第 40-84 行的依赖声明，改为类字段）：
- `_provider` — IServiceProvider? 类型，DI 注入构造函数传入时为 null（不自建容器），无参构造函数自建容器时为非 null
- `_options` — CallCenterOptions 类型
- `_chatClient` — IChatClient（pipeline-wrapped）
- `_agentFactory` — AIAgentFactory
- `_sessionStore` — InMemorySessionStore
- `_checkpointManager` — CheckpointManager（使用 CheckpointManager.Default）
- `_auditLogger` — AuditLogger
- `_eventBus` — IBusinessEventBus
- `_logger` — JsonlLogger
- `_refundWorkflow` — Workflow
- `_skillsProvider` — AgentSkillsProvider
- `_inputChannel` — Channel&lt;string&gt;（Interaction.cs 使用）
- `_recognizeIntent` — Func&lt;string, CancellationToken, Task&lt;IntentResult?&gt;&gt;（委托给 EntryPoint 的 RecognizeIntentAsync）
- `_disposed` — bool 类型，用于 IDisposable

**无参构造函数** `CallCenterService(CallCenterOptions? options = null)`（per D-10-01）：
1. 保存 `_options = options ?? new CallCenterOptions()`
2. 新建 ServiceCollection
3. 调用 `services.AddCallCenter(options)` — 注册 LLM、SessionStore、AuditLogger、EventBus、Mock 服务、Workflow
4. 调用 `services.AddSingleton<AIAgentFactory>()` — 注册 Agent 工厂
5. 调用 `services.AddSingleton<AgentSkillsProvider>()` — 注册技能提供者
6. BuildServiceProvider → `_provider`
7. 从 provider resolve 所有字段：`_chatClient`, `_agentFactory`, `_sessionStore`, `_auditLogger`, `_eventBus`, `_logger`, `_refundWorkflow`, `_skillsProvider`
8. `_checkpointManager = CheckpointManager.Default`
9. 创建 `_inputChannel = Channel.CreateUnbounded<string>()`
10. 启动 stdin 读取后台 Task（与 Program.cs 第 97-105 行相同逻辑）
11. 构建 `_recognizeIntent` 委托：使用 `_agentFactory.CreateIntentAgent(_skillsProvider)` 创建 intentAgent，调用其 RunAsync + StructuredOutputParser 解析

**DI 注入构造函数** `CallCenterService(IServiceProvider provider)`（per D-10-01）：
1. `_provider = null`（不自建，外部管理生命周期）
2. 从 provider resolve 所有字段
3. `_checkpointManager = CheckpointManager.Default`
4. `_inputChannel` 和 `_recognizeIntent` 也需要初始化（与无参构造函数相同逻辑）

**IDisposable 实现**：
- `Dispose()` 方法：如果 `_provider != null`（说明是无参构造函数自建的），调用 `_provider.Dispose()`
- 设置 `_disposed = true`

**文件头部**包含 `#pragma warning disable MAAI001`（与 Program.cs / EntryPoint.cs 保持一致，因为使用 Microsoft.Agents.AI 实验性 API）。

使用 `using` 语句导入所有需要的命名空间（参照 Program.cs 第 1-19 行）。
  </action>
  <verify>
    <automated>dotnet build src/CallCenter.AgentHost/CallCenter.AgentHost.csproj 2>&1 | grep -i "error" | wc -l == 0</automated>
    <manual>grep -c "public partial class CallCenterService" src/CallCenter.AgentHost/CallCenterService.Core.cs == 1</manual>
  </verify>
  <acceptance_criteria>
    - 文件 src/CallCenter.AgentHost/CallCenterService.Core.cs 存在且包含 `public partial class CallCenterService : IDisposable`
    - 文件包含两个构造函数签名：`CallCenterService(CallCenterOptions? options = null)` 和 `CallCenterService(IServiceProvider provider)`
    - 无参构造函数内包含 `services.AddCallCenter(` 和 `services.AddSingleton<AIAgentFactory>()`
    - DI 构造函数内 `_provider = null` 赋值存在
    - 文件包含 `public void Dispose()` 方法
    - Dispose 方法内包含 `if (_provider != null)` 条件检查
    - `dotnet build src/CallCenter.AgentHost/CallCenter.AgentHost.csproj` 无编译错误
  </acceptance_criteria>
  <done>Core.cs 文件存在，编译通过，双构造函数和 IDisposable 实现完整</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Console.In → CallCenterService | 用户输入直接进入服务层，无特殊安全边界（控制台场景） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | CallCenterService constructor DI | mitigate | 无参构造函数自建 DI 容器时，依赖 Extensions.cs 的安全输入层（SafetyInputDelegatingClient）已在管道中 |
| T-10-SC | Tampering | NuGet package installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- dotnet build 通过，无编译错误
- 文件中存在 partial class 声明
- 两个构造函数签名正确
- IDisposable 实现完整
</verification>

<success_criteria>
- CallCenterService.Core.cs 创建在 src/CallCenter.AgentHost/ 下
- partial class 声明包含 `: IDisposable`
- 无参构造函数自建 DI 容器（AddCallCenter + BuildServiceProvider）
- DI 构造函数 `_provider = null`
- Dispose 释放自建的 ServiceProvider
- 编译通过，AgentHost 项目无错误
</success_criteria>

<output>
Create `.planning/phases/10-callcenter-service-skeleton/10-01-SUMMARY.md` when done
</output>
