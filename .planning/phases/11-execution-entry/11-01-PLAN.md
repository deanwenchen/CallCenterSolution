---
phase: 11-execution-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/CallCenter.AgentHost/CallCenterService.Core.cs
  - src/CallCenter.ConsoleDemo/Program.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Program.cs is ~25-30 lines: using CallCenterService, while loop, ProcessAsync, print, Dispose"
    - "RunWorkflow/ResumeWorkflow/HandleRequestAsync static methods are deleted from Program.cs"
    - "All infrastructure assembly (LLM, Pipeline, AuditLogger, EventBus, Workflow) is inside CallCenterService"
    - "RefundCompletedEvent EventBus subscription fires when refund workflow completes"
    - "sessionId is fixed string 'demo-session'"
  artifacts:
    - path: "src/CallCenter.AgentHost/CallCenterService.Core.cs"
      provides: "EventBus subscription in both constructors"
      contains: "eventBus.Subscribe<RefundCompletedEvent>"
    - path: "src/CallCenter.ConsoleDemo/Program.cs"
      provides: "Minimal main loop (~25-30 lines)"
      min_lines: 20
  key_links:
    - from: "src/CallCenter.ConsoleDemo/Program.cs"
      to: "src/CallCenter.AgentHost/CallCenterService.Core.cs"
      via: "new CallCenterService() constructor"
      pattern: "new CallCenterService\\(\\)"
    - from: "src/CallCenter.ConsoleDemo/Program.cs"
      to: "src/CallCenter.AgentHost/CallCenterService.Intent.cs"
      via: "svc.ProcessAsync call"
      pattern: "ProcessAsync\\("
---

<objective>
将 Program.cs 从 ~440 行精简为 ~25-30 行主循环，并将 EventBus 订阅迁入 CallCenterService 构造函数。

Purpose: 完成 CallCenterService 提取的最后一步 —— Program.cs 不再承担基础设施组装和工作流驱动职责，仅作为服务的消费者入口。
Output: 精简后的 Program.cs + 带 EventBus 订阅的 Core.cs
</objective>

<execution_context>
@D:/Claude/CallCenterSolution1/.claude/get-shit-done/workflows/execute-plan.md
@D:/Claude/CallCenterSolution1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-execution-entry/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 Core.cs 两个构造函数中添加 RefundCompletedEvent EventBus 订阅</name>
  <read_first>
    - src/CallCenter.AgentHost/CallCenterService.Core.cs — 修改目标
    - src/CallCenter.ConsoleDemo/Program.cs — 第 49-53 行原始订阅代码（参照）
  </read_first>
  <acceptance_criteria>
    <behavior>CallCenterService 实例化后，RefundCompletedEvent 发布时控制台输出 "[EVENT] 退款完成: 订单{OrderId}, 金额 {RefundAmount:C}"</behavior>
    <source_assertion>CallCenterService.Core.cs 参数less构造函数中，_eventBus 解析之后（当前第 74 行之后）包含 eventBus.Subscribe&lt;RefundCompletedEvent&gt; 调用</source_assertion>
    <source_assertion>CallCenterService.Core.cs DI构造函数中，_eventBus 解析之后（当前第 112 行之后）包含 eventBus.Subscribe&lt;RefundCompletedEvent&gt; 调用</source_assertion>
    <source_assertion>订阅回调输出格式: $"\n[EVENT] 退款完成: 订单{e.OrderId}, 金额 {e.RefundAmount:C}"（与 Program.cs 原始完全一致）</source_assertion>
    <cli>dotnet build src/CallCenter.AgentHost/CallCenter.AgentHost.csproj 编译通过，无新增错误</cli>
  </acceptance_criteria>
  <action>
    在 CallCenterService.Core.cs 的两个构造函数中，在 _eventBus 字段解析之后添加 EventBus 订阅代码。

    【参数less构造函数（第 74 行之后）】
    在 `_eventBus = _provider.GetRequiredService&lt;IBusinessEventBus&gt;();` 之后、`_logger` 解析之前，插入：
    
    _eventBus.Subscribe&lt;RefundCompletedEvent&gt;(e =&gt;
    {
        Console.WriteLine($"\n[EVENT] 退款完成: 订单{e.OrderId}, 金额 {e.RefundAmount:C}");
        return Task.CompletedTask;
    });

    【DI构造函数（第 112 行之后）】
    在 `_eventBus = provider.GetRequiredService&lt;IBusinessEventBus&gt;();` 之后、`_logger` 解析之前，插入相同的订阅代码。

    注意：
    - 使用同步 Action 回调（非 async），与 Extensions.cs AddCallCenter 中 OnRefundCompleted 的模式一致
    - 不需要 await Task.CompletedTask，直接 return Task.CompletedTask 即可（InMemoryBusinessEventBus 接受 Func&lt;T, Task&gt;）
    - 确保 using CallCenter.Workflows.Refund; 已在文件顶部存在（RefundCompletedEvent 在此命名空间）—— 已在第 14 行

    完成后编译验证：dotnet build src/CallCenter.AgentHost/CallCenter.AgentHost.csproj
  </action>
  <verify>
    <automated>dotnet build src/CallCenter.AgentHost/CallCenter.AgentHost.csproj 2>&amp;1 | grep -c "error"</automated>
  </verify>
  <done>Core.cs 两个构造函数都包含 RefundCompletedEvent 订阅；编译通过无错误；订阅回调输出格式与 Program.cs 原始一致</done>
</task>

<task type="auto">
  <name>Task 2: 重写 Program.cs 为极简主循环（~25-30 行）</name>
  <read_first>
    - src/CallCenter.ConsoleDemo/Program.cs — 修改目标（当前 440 行，全部替换）
    - src/CallCenter.AgentHost/CallCenterService.Core.cs — 确认构造函数签名（CallCenterService(CallCenterOptions?) 无参可用）
    - src/CallCenter.AgentHost/CallCenterService.Intent.cs — 确认 ProcessAsync 签名
  </read_first>
  <acceptance_criteria>
    <behavior>运行 dotnet run --project src/CallCenter.ConsoleDemo/CallCenter.ConsoleDemo.csproj 显示 "=== CallCenter AI Demo ===" banner</behavior>
    <behavior>输入 "quit" 或空行 → 程序退出</behavior>
    <behavior>输入业务消息 → 调用 svc.ProcessAsync → 输出结果字符串到控制台</behavior>
    <source_assertion>Program.cs 不包含 static async Task RunWorkflow( 方法定义</source_assertion>
    <source_assertion>Program.cs 不包含 static async Task ResumeWorkflow( 方法定义</source_assertion>
    <source_assertion>Program.cs 不包含 static async Task HandleRequestAsync( 方法定义</source_assertion>
    <source_assertion>Program.cs 不包含 new OpenAIClient( 或 IChatClient 创建代码</source_assertion>
    <source_assertion>Program.cs 不包含 StandardPipelineFactory.CreatePipeline 调用</source_assertion>
    <source_assertion>Program.cs 不包含 Channel.CreateUnbounded 或 inputChannel 变量</source_assertion>
    <source_assertion>Program.cs 不包含 refundWorkflow 或 refundWorkflowWithAudit 变量</source_assertion>
    <source_assertion>Program.cs 包含 string sessionId = "demo-session"</source_assertion>
    <source_assertion>Program.cs 包含 new CallCenterService() 实例化</source_assertion>
    <source_assertion>Program.cs 包含 svc.ProcessAsync(sessionId, userMessage) 调用</source_assertion>
    <source_assertion>Program.cs 包含 using 声明或 Dispose 调用确保服务释放</source_assertion>
    <source_assertion>Program.cs 行数 ≤ 35 行（留 5 行余量）</source_assertion>
    <source_assertion>Program.cs using 引用仅包含: System, CallCenter.AgentHost（无 OpenAI, Microsoft.Agents.AI, System.Threading.Channels 等）</source_assertion>
    <cli>dotnet build src/CallCenter.ConsoleDemo/CallCenter.ConsoleDemo.csproj 编译通过，无新增错误</cli>
  </acceptance_criteria>
  <action>
    将 src/CallCenter.ConsoleDemo/Program.cs 全部内容替换为以下极简主循环（约 25-30 行）：

    文件结构：
    1. #pragma warning disable MAAI001
    2. using System;
    3. using CallCenter.AgentHost;
    4. 空行
    5. using var svc = new CallCenterService();
    6. var sessionId = "demo-session";（per D-11-03）
    7. 空行
    8. Console.WriteLine("=== CallCenter AI Demo ===");
    9. Console.WriteLine("输入消息开始（如'我要退款，订单A001'），输入'quit'退出。\n");
    10. 空行
    11. while (true)
    12. {
    13.     Console.Write("用户: ");
    14.     var userMessage = Console.ReadLine();
    15.     if (string.IsNullOrWhiteSpace(userMessage) || userMessage.Trim().Equals("quit", StringComparison.OrdinalIgnoreCase)) break;
    16.     var result = await svc.ProcessAsync(sessionId, userMessage);
    17.     Console.WriteLine($"系统: {result}");
    18. }

    关键决策：
    - 使用 Console.ReadLine() 直接读取（per D-11-01）—— 不再需要 Channel + 后台 Task，因为 CallCenterService 构造函数已在内部创建 _inputChannel（仅供 HandleRequestAsync 中订单号/确认输入使用，ProcessAsync 路径不读取控制台）
    - ProcessAsync 返回 string 直接打印（per D-11-01）—— 不再有 switch dispatch，所有路由逻辑已在 CallCenterService.Intent.cs 的 ProcessAsync 内完成
    - 使用 using var 声明 svc（per D-11-01）—— 退出时自动调用 Dispose()
    - 退出条件保留 quit + 空行检查（per CONTEXT.md Claude's Discretion）
    - Banner 格式保持 "=== CallCenter AI Demo ==="（per CONTEXT.md Claude's Discretion）
    - 删除所有静态方法（per D-11-02）：RunWorkflow、ResumeWorkflow、HandleRequestAsync
    - 删除所有基础设施代码（per D-11-01）：OpenAIClient、Pipeline、AuditLogger、EventBus、Workflow、SessionStore、CheckpointManager
    - 清理所有未使用的 using 引用

    替换方法：使用 Write 工具完整覆盖 Program.cs 文件内容。
  </action>
  <verify>
    <automated>dotnet build src/CallCenter.ConsoleDemo/CallCenter.ConsoleDemo.csproj 2>&amp;1 | grep -c "error"</automated>
  </verify>
  <done>Program.cs 精简为 ~25-30 行；不含任何静态方法或基础设施组装代码；编译通过；banner/退出条件/sessionId 与决策一致</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Console In → Program.cs | 用户输入未经校验直接进入 ProcessAsync |
| Program.cs → CallCenterService.ProcessAsync | 主循环直接传递用户消息，无额外过滤 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Spoofing | Console.ReadLine → ProcessAsync | accept | Demo 环境，无身份验证要求；sessionId 固定为 "demo-session" |
| T-11-02 | Tampering | Program.cs 精简后删除的静态方法 | accept | 功能已迁移到 CallCenterService，删除不会引入新攻击面 |
| T-11-03 | Information Disclosure | Console.WriteLine of ProcessAsync result | accept | Demo 输出到本地控制台，不通过网络传输 |
| T-11-SC | Tampering | npm/pip/cargo installs | N/A | No package manager installs in this phase — pure C# refactoring |
</threat_model>

<verification>
## 整体验证

1. 编译全解决方案：`dotnet build` 无新增错误或警告
2. 端到端冒烟测试：`dotnet run --project src/CallCenter.ConsoleDemo/CallCenter.ConsoleDemo.csproj` → 输入 "你好" → 看到问候语输出
3. 源码断言：Program.cs 行数 ≤ 35，无 static 方法定义
4. EventBus 验证：Core.cs 包含两个 eventBus.Subscribe&lt;RefundCompletedEvent&gt; 调用
</verification>

<success_criteria>
- [ ] Core.cs 两个构造函数都包含 RefundCompletedEvent 订阅
- [ ] Program.cs 精简为 25-35 行极简主循环
- [ ] Program.cs 中无 RunWorkflow/ResumeWorkflow/HandleRequestAsync 静态方法
- [ ] Program.cs 中无 OpenAIClient/Pipeline/AuditLogger/EventBus/Workflow 基础设施代码
- [ ] Program.cs 中无 Channel/inputChannel 变量
- [ ] Program.cs 中无 refundWorkflow/refundWorkflowWithAudit 变量
- [ ] sessionId = "demo-session" 固定字符串
- [ ] Program.cs 使用 `using var svc = new CallCenterService()` + while + ProcessAsync + Dispose
- [ ] 全解决方案编译通过，无新增错误
- [ ] 冒烟测试：输入"你好"得到问候语输出
</success_criteria>

<output>
Create `.planning/phases/11-execution-entry/11-01-SUMMARY.md` when done
</output>
